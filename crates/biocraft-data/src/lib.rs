//! biocraft-data — L2: Veri katmanı stub (MK-31, MK-33, MK-34).
//!
//! SQLite (config/meta) + DuckDB (analitik) + RocksDB (KV/cache) + proje formatı.
// MK-40: L2 katmanı — yalnızca L0/L1 katmanlarına bağlı; üst katman yasak.

pub use biocraft_sdk;
pub use biocraft_types;
