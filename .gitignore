# Rust / Cargo
/target/
# Cargo.lock: binary workspace için repoda tutulur (MK: Cargo.lock kilitli)
**/*.rs.bk
*.pdb

# Editör — VS Code
.vscode/
*.code-workspace

# Editör — JetBrains
.idea/
*.iml

# OS — Windows
Thumbs.db
Desktop.ini
*.lnk

# OS — macOS
.DS_Store
.AppleDouble
.LSOverride

# OS — Linux
*~

# Gizli anahtarlar / ortam dosyaları (asla repoya girmesin — MK-44/MK-60)
.env
.env.*
secrets.*
*.pem
*.key
*.p12
*.pfx

# Derleme çıktıları
*.exe
*.dll
*.so
*.dylib

# Log dosyaları
*.log
logs/

# Test çıktıları / golden farklar (büyük dosyalar için)
test_output/

# İP-21: benchmark çalıştırma çıktısı (üretilen; baseline.json izlenir, current.json izlenmez)
benchmarks/current.json
bench-results.json

# Geçici manuel test/smoke çıktıları (render host doğrulaması — stderr/stdout yönlendirmesi)
smoke*.err
smoke*.out
tdr_test.*

# BioCraft proje dosyaları (örnek / geçici)
*.bcproj
*.bcext
!assets/**/*.bcext

# Sihirbazla/uygulamayla depo kökünde oluşturulan manuel TEST projeleri (repoya girmesin).
# Bir BioCraft projesi = içinde biocraft.toml + .biocraft_meta/ olan klasördür; deneme amaçlı
# oluşturulanları burada listele.  (İleride yeni test projeleri için aynı satırın altına ekle.)
/sad/

# Node (yardımcı araçlar için)
node_modules/
dist/
