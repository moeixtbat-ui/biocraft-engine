# PROGRESS.md — BioCraft Engine İlerleme Günlüğü

> Bu dosya BioCraft Engine'in **hafızasıdır.** Yapay zeka her oturum sonunda buraya bir satır ekler.
> Yeni bir bağlam (context) açtığında, yapay zeka önce `git log` çalıştırır + bu dosyayı okur,
> böylece "şimdiye kadar ne yaptık, nerede kaldık" sorusunu kullanıcıya geçmiş yapıştırtmadan yanıtlar.
>
> **Kural (yapay zeka için):** Her commit'ten ÖNCE aşağıdaki tabloya o günün satırını ekle.

---

## Mevcut Durum (Özet)

- **Marka:** BioCraft Engine · biocraftengine.com
- **Aktif sürüm hedefi:** MVP — tam kullanılabilir ilk sürüm (motor + çekirdek eklenti BioCraft Studio + AI yüzeyi)
- **Kapsam:** Temel uygulama İP-00…İP-21 + Çekirdek eklenti ÇE-00…ÇE-12 + AI yüzey (İP-14 / YZ-00,01,06,08)
- **Son tamamlanan gün:** Gün 22 — **İP-06 Kod Editörü ve Hibrit Köprü — 1. kısım (MK-55, MK-02). FAZ 3 sürüyor.** Native kod editörünün çekirdeği kuruldu — **Monaco/web YOK, tamamen egui**. **Önemli karar:** söz dizimi vurgulaması, proje sahibinin onayıyla **saf Rust** yazıldı (Tree-sitter C kütüphanesi **eklenmedi** → projenin "**C derleyici GEREKMEZ**" ilkesi korundu; `Vurgulayici` trait ile Tree-sitter **v1.x'te takılabilir**). **Yeni `biocraft-plugin-host/src/exec/` (mod.rs + python.rs):** **out-of-process kod çalıştırma motoru (MK-02)** — kullanıcı kodu **AYRI SÜREÇTE** çalışır (`python -u`, `PYTHONUTF8`); `calistir_baslat` **ANINDA döner** → **sonsuz döngü bile arayüzü dondurmaz**; 3 iş parçacığı (stdout/stderr okuyucu + gözlemci); **`durdur()` süreci öldürür** ("Durdur" her an), **zaman aşımı gerçekten uygulanır (kill)**, çıktı **satır satır akışla** kanaldan gelir (**kayıpsız UTF-8 decode** → Windows cp1252 satır düşürmez); `python_bul` paylaşılır (Python yoksa **in-process'e DÖNMEZ**, "[Kur]" yönlendirmesi). **Yeni `biocraft-ui/src/editor/` (mod/syntax/tree/run + tests):** **`syntax.rs` = saf-Rust ARTIMLI vurgulayıcı:** `KodDili` (Python öncelik + R/Bash/JSON/YAML/RON), `Vurgulayici` trait (Tree-sitter kancası), `BasitVurgulayici` (anahtar kelime/dize/yorum/sayı/operatör + **Python üç-tırnak çok-satır durum makinesi**), `JetonTuru::token_anahtari` (**MK-52 — sabit RGB yok, renk token'dan**), **`VurgulamaOnbellek`** = **yalnız değişen satırı yeniden jetonlar** (büyük dosyada akıcılık = MK-04). **`mod.rs`:** `MetinTampon` (Düzenlenebilir `String` **veya** `AkisGoruntuleyici`); **`AkisGoruntuleyici` = out-of-core (MK-09):** **memmap2** + kaba satır-ofset indeksi (her 1000 satırda bir) → **1 GB log RAM'e ALINMADAN** açılır, yalnız **görünür pencere** (`show_rows`) çizilir; `Belge` (8 MiB üstü dosya otomatik **akışla salt-okunur**); `KodEditoru` (sekme + proje ağacı + kod alanı + **çıktı paneli** + **▶Çalıştır / ▶Hücre / ■Durdur** araç çubuğu; düzenlenebilir kısım egui `TextEdit` + söz dizimi `layouter`'ı → imleç/seçim/geri-al bedava; **`# %%` hücre seçimi imleçten**). **`tree.rs`:** **tembel** proje dosya ağacı (genişletilene dek dizin okunmaz). **`run.rs`:** arayüz tarafı `CalistirmaDurumu` (her kare `yokla` ile **bloklamadan** çıktı toplar MK-07, `durdur`, 5000-satır taşma koruması). **app entegrasyonu:** `KabukAksiyon::KodEditoru` (Görünüm menüsü) → merkez bölgede tam editör (node/galeri ile **karşılıklı dışlamalı**); `Sahne.kod_editoru = KodEditoru::ornek()` örnek Python akışıyla açılır. **Yeni dış bağımlılık YOK** (biocraft-ui→biocraft-plugin-host = L4→L3 [render gibi, döngü yok]; memmap2 zaten workspace ağacında). **612 test** (types 29 + sdk 11 + ipc 3 + ui **189** + render 50 + mem 63 + state 50 + plugin-host **96** + launcher 33 + data 88). Çalışma-zamanı: Python bu makinede mevcut → exec **uçtan uca testler CANLI geçti** (print çıktısı akar + Bitti(0); **sonsuz döngü başlatma bloklamaz + Durdur→Durduruldu**; **zaman aşımı→kill**; hatalı kod çıkış kodu 3). fmt/clippy(-D)/machete/topology/**cargo-deny (exit 0)** temiz. **NOT (Gün 23):** node↔kod köprüsü (`bridge.rs`); temel LSP (pyright/jedi, out-of-process, `lsp.rs`); izole ortam (proje sanal ortamı + paket arama/kurma + sürüm kilidi); kaydet'te ruff/black biçimlendirme. **Ertelenen:** `ropey` rope + tam özel render (çok büyük **düzenlenebilir** dosya için v1.x; büyük salt-okunur zaten akışla); tam LSP/debugger v1.x. (2026-06-22)
- **Önceki gün (21):** Gün 21 — **İP-05 Node Motoru (Görsel Akış Sistemi) — 2. kısım: İP-05 TAMAMLANDI (MK-54).** Node motoruna **paralel çalıştırma + sonuç önbelleği + `.bcflow` kayıt + eklenti SDK node kaydı + PNG/SVG + node→Python** eklendi. **`biocraft-sdk/src/node.rs` kontratı genişledi:** parametre şeması (`ParametreTuru`/`ParametreTanimi`/`ParametreDeger`/`Parametreler` — kararlı imza), **`AkisDeger`** (kablolarda akan **tipli+özetli+boyutlu** değer; özet = kablo önizleme, bayt = bütçe), **`NodeCalistirici`** trait (`Send+Sync`; `calistir`/`agir_mi`/`tahmini_bayt`), `NodeKaydi`; `NodeTanimi`'ye serde-default kategori/açıklama/parametreler (eski tanım uyumlu). **`biocraft-ui/src/node/` 4 yeni dosya:** **`run.rs` = ÇALIŞTIRMA MOTORU:** topolojik **dalga** ilerlemesi; bağımsız dallar **`std::thread::scope` ile PARALEL** (Rayon/Tokio değil — proje az-bağımlılık ilkesi, `petgraph`'sız DAG'le aynı çizgi); her node çalışmadan önce **bellek orkestratöründen rezervasyon** (MK-21/22), bir dalgadaki eş zamanlı node'lar **bütçeye sığacak gruplara** bölünür → büyük akışta bile **OOM YOK** (tek node bile sığmazsa o node zarif **Hata**, çökme değil); **hata o dalı durdurur, bağımsız dallar devam eder** (durum halkası Bitti/Hata/atlandı); **iptal jetonu** (dalga öncesi denetlenir) + **ilerleme olayı** (her node bittikçe bildirilir); `YurutucuKayit::ornek()` 7 demo çalıştırıcı (eklenti node'ları SDK ile kaydolur). **`cache.rs` = SONUÇ ÖNBELLEĞİ:** node imzası = tür + parametre imzası + tüm girdi (yukarı-akış çıktı) imzaları; imza aynıysa **yeniden hesaplanmaz**, bir parametre/yukarı-akış değişince imza değişir → o node + **alt-graf kendiliğinden** yeniden hesaplanır (bayat sonuç yok). **`serialize.rs` = KAYIT:** **`.bcflow` (JSON)** = sürüm + node id + bağlantı + parametre (git-diff alınır) + **göç fonksiyonu** (MK-59 — daha yeni sürüm **reddedilir**, sessiz/bozuk yükleme yok); **SVG** (modelden **tam vektör**: metin+renk+kablo) + **saf-Rust minimal PNG** kodlayıcı (yazılım rasterizer + stored-deflate + CRC-32/Adler-32 — `.bcproj` ZIP yazıcısıyla aynı ruh, **dış bağımlılık YOK**; glyph fontu gömülmez, metin SVG'de). **`kod.rs` = Node→Kod:** temel akış **eşdeğer Python betiği** (iskelet fonksiyon + topolojik çağrı + parametre kwargs; **Kod→Node ters yön YOK**, v1.x). **`canvas.rs` entegrasyonu:** araç çubuğuna **▶ Çalıştır** (arka plan `std::thread` + kare-başı `try_recv` yoklama → **arayüz DONMAZ** MK-07) + **İptal**/ilerleme çubuğu + **Canlı mod** (ağır node uyarılı) + canlı durum halkaları + **kabloya tıkla → ara veri önizleme** + **Dışa Aktar** menüsü (.bcflow/SVG/Python panoya — `rfd` dosya seçici hâlâ ertelendi) + F5 kısayolu; senkron `calistir_simdi` (test/basit kullanım). App'in Görünüm→Node Editörü menüsü zaten bu zenginleşmiş tuvali açar (tuval kendi 1 GiB bellek orkestratörünü taşır; app paylaşılan orkestratör enjekte edebilir). **Yeni dış bağımlılık YOK** (serde/serde_json zaten workspace ağacında → cargo-deny ağacı değişmedi). **571 test** (sdk 11 + ui 154 + ipc 3 + types 29 + render 50 + mem 63 + state 50 + plugin-host 90 + launcher 33 + data 88). Demo: `cargo run -p biocraft-ui --example node_calistir_demo` (8 node 2 bağımsız dal paralel+bütçe; 2. çalıştırma 8/8 önbellek; parametre→2 yeniden+6 önbellek; .bcflow 3793 bayt gidiş-dönüş; PNG/SVG/Python dosyaya). **NOT (Gün 22+):** node parametre düzenleme paneli (şema→form, anlık doğrulama); PNG sıkıştırma (stored→gerçek deflate); `rfd` ile gerçek dosya kaydet/aç; node gruplama/subgraph derinleştirme. (2026-06-22)
- **Önceki gün (20):** Gün 20 — **İP-05 Node Motoru (Görsel Akış Sistemi) — 1. kısım (MK-54). FAZ 3 BAŞLADI.** Unreal Blueprint mantığında node tabanlı görsel akış motorunun çekirdeği + tuvali kuruldu. **Yeni `biocraft-ui/src/node/` modülü 7 dosya** (`port`+`graph`+`dag`+`commands`+`katalog`+`canvas`+`tests`). **`port.rs` = TİPLİ & RENKLİ PORTLAR:** `VeriTuru` (kanonik kimlik; `uyumlu_mu` = aynı tür **veya** joker "her") + `Port` + **`tur_renk_anahtari`** (FNV-1a hash → kararlı token anahtarı; somut renk **token'dan** = MK-52, sabit RGB YOK; joker nötr) + `DonusturucuKayit::oner` (tip uyumsuzsa **otomatik dönüştürücü önerisi**). **`graph.rs` = SAF MODEL (egui'siz, testlenir):** `Node{kimlik,tur,başlık,konum,giriş/çıkış portları,durum}` + `NodeDurumu` (Bekliyor/Çalışıyor/Bitti/Hata = **durum halkası**) + `Baglanti`(çıkış→giriş) + `YapiskanNot` + `PortRef`; **`NodeGraf`** ekle/kaldır/taşı (ham mutasyonlar) + **`baglanti_kontrol`** = bağlantı doğrulama kalbi: yön (çıkış→giriş) + aynı-node reddi + **tip uyumu** (uyumsuz→öneri taşır) + **giriş tekildir** (fan-in yok, çıkış fan-out serbest) + zaten-var + **DAG döngü reddi**. **`dag.rs` = DÖNGÜ KISITI (`petgraph`'sız):** `yol_var_mi` (DFS) + `topolojik_sira` (Kahn; döngüde None) + `dongu_var_mi`; `out(A)→in(B)` kenarı B→A yolu varsa **döngü** = reddedilir. **`commands.rs` = UNDO/REDO (İP-11 `Komut<NodeGraf>` motoru, MK-36/37):** NodeEkle/NodeSil(bağlı kenarları da yakalar+geri yükler)/NodeTaşı(simetrik)/BağlantıEkle/BağlantıSil/NotEkle/NotSil; hepsi **tek mantıksal depoya** (`node-graf:<id>`) dokunur (MK-37); önceden-ayrılmış kimlikle **kararlı yinele**. **`katalog.rs`:** `NodeKatalogu` (palet içeriği + arama) + 7 demo genomik node türü (Dizi Oku→Hizala→Varyant Çağır→Varyant→Tablo→Filtrele→Özet/3B). **`canvas.rs` = egui TUVALİ:** pan (boş-alan/orta-tuş), **zoom** (tekerlek, imleç sabit), **minimap** (genel bakış + görünür-alan + tıkla-ortala), **tümünü sığdır**, arka plan ızgarası; **sağ-tık → aranabilir palet** + araç çubuğu (＋Node/＋Not/geri-al/yinele/zoom%); **portlardan sürükleyerek bağlama** (sürükleme sırasında **uyumlu girişler vurgulanır**, uyumsuz → **anlık uyarı şeridi** + dönüştürücü önerisi); node sürükle-taşı (bırakınca undo komutu) + sağ-tık Sil + Delete tuşu; bağlı girişe tıkla → bağlantı sök; **durum halkası** (kenarlık rengi); **culling** (ekran-dışı çizilmez) + **LOD** (düşük zoomda port/metin atlanır) = MK-04 kare bütçesi. **app entegrasyonu:** `KabukAksiyon::NodeEditoru` (Görünüm menüsü) → merkez bölgede tam-tuval (galeri gibi); `Sahne.node_tuvali: NodeTuvali::ornek()` demo akışla açılır. **Yeni dış bağımlılık YOK** (egui + biocraft-state undo motoru + std). **537 test** (ui 90→**124** +34: model/DAG/undo + headless egui çizim panik-yok). **NOT (Gün 21):** paralel çalıştırma + sonuç önbelleği + `.bcflow` (RON/JSON) kaydı + eklenti **SDK node kaydı** (`biocraft-sdk`) + PNG/SVG dışa aktarma. (2026-06-22)
- **Önceki gün (19):** Gün 19 — **İP-09 Güvenlik, Şifreleme ve Sandbox Sertleştirme — çekirdek güvenlik katmanı (MK-44/45/20). FAZ 2 TAMAMLANDI (İP-01/02/10/09 ✅).** Güvenlik kodu **AÇIK/denetlenebilir** (Kerckhoffs — MK-20); kapalı olan yalnız lisans/anti-tamper (bu MVP'de yok). **`biocraft-data`'ya yeni `src/security/` modülü 6 dosya:** **`crypto.rs` = DİNLENMEDE ŞİFRELEME (MK-44):** `VeriAnahtari` (256-bit; `#[derive(Zeroize,ZeroizeOnDrop)]` → drop'ta bellekten **sıfırlanır**, Debug/Clone bilinçli türetilmedi = anahtar loglanamaz/çoğalamaz) + `SifreliVeri{nonce:[u8;12],sifreli_metin}` (serde zarf, `duz_bayt`/`duz_bayttan`); **`sifrele`** AES-256-GCM + **her çağrıda TAZE rastgele nonce** (OS CSPRNG getrandom → aynı anahtarla nonce ASLA tekrar etmez = GCM güvenliği); **`coz`** kimlik-doğrulamalı (yanlış anahtar/kurcalanmış veri → **reddedilir**, GCM etiketi tutmaz). **`credentials.rs` = OS GÜVENLİ DEPO (MK-44):** `KimlikDeposu` trait (proje deseni) + `OsKimlikDeposu` (**keyring**: Windows Credential Manager / macOS Keychain / Linux kernel keyutils — D-Bus YOK) + `BellekKimlikDeposu` (test/CI ikilisi); sırlar hex-kodlu (ham 256-bit anahtar ek bağımlılık olmadan saklanır); anahtar **asla kodda/düz metinde değil**; `anahtar_kur_veya_yukle` (ilk çağrıda üret+sakla, sonra geri yükle = kalıcı). **`secure_delete.rs` = GÜVENLİ SİLME (MK-45):** `dosya_uzerine_yaz` (1..N geçiş sıfır/rastgele + fsync, 1 MiB pencere) + `guvenli_dosya_sil`; **dürüst not:** SSD'de üzerine-yazma garanti değil → gerçek garanti = at-rest şifreleme + anahtar imhası (kripto-shred). `privacy/export.rs`'in `guvenli_sil`'i artık bu primitifi kullanır (DRY). **`integrity.rs` = İMZALI GÜNCELLEME (MK-16/33):** `GuncellemeBildirimi{surum,blake3_hex,boyut}` + `guncelleme_dogrula` (Ed25519 resmi-anahtar imza **+** BLAKE3 checksum; **sahte/değiştirilmiş güncelleme reddedilir** — kabul kriteri); saf/test-edilebilir, gerçek indirme+TLS üst katman/İP-18. **`sanitize.rs` = LOG/PII TEMİZLEME (MK-45):** `pii_temizle` (e-posta/mutlak yol/7+ haneli rakam dizisi → `[gizlendi]`; regex'siz, boşluk yapısı korunur). **`biocraft-plugin-host`'a `harden.rs` = SANDBOX SERTLEŞTİRME (MK-12/13/15):** `AyristirmaLimitleri` (max girdi/çıktı bayt + max **şişme oranı** + max girdi sayısı; `siki()`) → **zip-bomb/kaynak istismarı** açma ÖNCESİ reddedilir; `cikti_denetle` **checked_add** (u64 taşması → panik YOK, net hata); `cekirdek_arg_dogrula` (boş/uzun/NUL/denetim karakteri reddi); `SurecSinirlari` (OS-düzeyi RAM/CPU tavanı — Windows Job Object/Linux cgroup **kancası**, `native_zorlama_aktif()=false`, İP-09 sonrası). `BcextPaket::ac` artık `AyristirmaLimitleri::siki()` ile devasa/çok-girdili paketi reddeder (Okuyucu zaten checked + bellek-güvenli). **`fuzz/` = FUZZING İSKELETİ (MK-58):** workspace-DIŞI ayrı crate (kök `exclude=["fuzz"]` → normal build/test/clippy/machete/deny ETKİLENMEZ); 4 cargo-fuzz hedefi (manifest_toml/bcp1_zarf/bcext_paket/sifreli_zarf) + README (`cargo +nightly fuzz run ...`); cargo-audit zaten CI'da (MK-60). **Yeni dış bağımlılık:** aes-gcm 0.10 + getrandom 0.2 + zeroize 1 (derive) + keyring 3 (default-features=false, yalnız native backend'ler) — HEPSİ saf Rust (RustCrypto), C derleyici YOK, Apache-2.0/MIT; ed25519-dalek (zaten workspace'te) biocraft-data'ya bağlandı. cargo-deny **exit 0** (advisories/bans/licenses/sources OK). **503 test** (data 58→**88** +30, plugin-host 82→**90** +8). Demo: `cargo run -p biocraft-data --example guvenlik_demo` (şifrele→çöz + yanlış anahtar/kurcalama reddi + güvenli silme + imzalı güncelleme sahte-red + log PII maskeleme **canlı**); sandbox sertleştirme `eklenti_host_demo [7]` (zip-bomb/devasa/taşma reddi canlı). **NOT (gelecek):** opsiyonel kullanıcı parolası KDF (Argon2 ile, güç-kullanıcı — şu an rastgele veri anahtarı esas); native OS süreç-limiti (Job Object/cgroup) zorlaması; gerçek auto-updater indirme+TLS (İP-18); fuzz CI job'u (nightly — şimdilik yerel/araç). (2026-06-22)
- **Önceki gün (18):** Gün 18 — **İP-10 Gizlilik & Veri Yönetimi — gizliliği ÇEKİRDEK seviyesinde uygula (MK-41/42/43/34).** Yeni `biocraft-data/src/privacy/` modülü 7 dosya (`classify`+`profile`+`consent`+`anonymize`+`provenance`+`lineage_browser`+`export`) + `mod`. **`classify.rs` = ÇIKIŞ KAPISI (kalp):** `DisKanal` (P2p/DisAi/DisApi/Telemetri/Paylasim) + `cikis_denetle(sinif,kanal)`→`CikisKarari` (PHI/hassas **mutlak `Engellendi`**, üst katman ayarı ASLA açamaz, atlanamaz); `en_kisitlayici` **fail-closed** (boş/bilinmeyen küme → PHI → bloklu); `kume_denetle` paket için en hassas öğeye göre. **Mimari kilit:** kapı L2'de (`biocraft-data`); dış kanallar (`biocraft-net`/`biocraft-ai-surface` L3) ona bağımlı → MK-40 ile kapının *altına inip atlayamaz*. **`profile.rs`:** `GizlilikProfili` (yerel-varsayılan: `tamamen_yerel=true`/telemetri **Kapalı**/AI havuzu **kapalı**/her dış gönderim onay); `proje_ile_coz` = projenin manifest `[gizlilik]`'i **global'i EZER** (İP-02 ile bağlı); `dis_kanal_etkin_mi` (`tamamen_yerel` → HİÇ dış kanal açık değil). **`consent.rs`:** ÜÇ KAPI sırayla (1 sınıf→2 profil→3 onay) `gonderim_degerlendir`→`GonderimDurumu` (`Engellendi`/`KanalKapali`/`OnayBekliyor`); `GonderimOzeti` **şeffaflık** (kaç öğe/hangi sınıf/ne kadar bayt/hangi alanlar gidiyor); `OnayKaydi` denetim izi. **`provenance.rs`:** `VeriKokeni` (kaynak/sürüm/tarih/**BLAKE3**/sınıf + opsiyonel `LisansAtif` lisans+atıf+url) + append-only `koken.jsonl` & `onaylar.jsonl` (her veri yüklemesi köken üretmeli; ÇE-01 ile tutarlı). **`lineage_browser.rs`:** `KokenGezgini` **saf model** (egui'siz; satırlar/ara/sınıfa-göre/detay/**`atiflar()` yöntem bölümü için tekilleştirilmiş**). **`export.rs` = KVKK/GDPR HAKLARI:** `veri_envanteri` (erişim/görüntüleme), `tam_veri_ihrac` (taşınabilirlik — manifest **FİLTRESİZ**, sahibinin tam kopyası, `.zip`), `guvenli_sil` (unutulma — opsiyonel üzerine-yaz + klasörü kaldır; **proje değilse reddet** = yanlış silme koruması; dürüst not: SSD'de gerçek garanti at-rest şifreleme/kripto-shred = İP-09). `anonymize.rs` = anonimleştirme **temeli** (doğrudan tanımlayıcı sil + yarı-tanımlayıcı genelleştir + **k-anonimlik** ölçümü + Laplace **diferansiyel gizlilik** sayım gürültüsü; gerçek sertleştirme v1.x). net/ai-surface stub'larına **çıkış-kapısı sözleşme notu** eklendi. **Yeni dış bağımlılık YOK** (std + mevcut serde/serde_json/toml/blake3/chrono). **465 test** (data 22→**58**, +36). Demo: `cargo run -p biocraft-data --example gizlilik_demo` (PHI 5 kanalda engel + yerel-varsayılan/proje-override + onay akışı + köken gezgini/atıf + envanter/ihraç/güvenli-silme **uçtan uca** canlı). (2026-06-21)
- **Önceki gün (17):** Gün 17 — İP-02 **Proje Formatı (2. kısım) — İP-02 ARTIK TAM (MK-31/33/34/59).** `biocraft-data` (L2) crate'i dolduruldu: yeni `project/` modülü 6 dosya (`mod.rs`+`format.rs`+`manifest.rs`+`integrity.rs`+`provenance.rs`+`export.rs`). **Proje = klasör** (`biocraft.toml` + `data/inputs,intermediate` + `flows/` + `scripts/` + `provenance/` + `.biocraft_meta/`). **`manifest.rs` = `biocraft.toml` zengin şeması:** kök seviyede yalnızca alt-tablolar (TOML "değerler tablolardan önce" tuzağından kaçınılır) → `[kimlik]` (ad/açıklama/**BioCraft sürümü**/**format sürümü** MK-59/şablon/oluşturma+değiştirme), `[olusturan]` (opsiyonel **ORCID**+kurum), `[siniflandirma]` (zorunlu **DataClassification** MK-42+uyumluluk etiketleri+lisans+etiketler), `[gizlilik]` (tamamen_yerel/ai_havuzu/dağıtık/**determinizm kancası**), **`[guvenlik]` HASSAS** (şifreleme — export'ta varsayılan HARİÇ madde 7), `[veri]` (yerleşim/büyük-veri/akış), `[[harici_veri]]` (mantıksal yol+**gerçek yol ipucu**+boyut+**BLAKE3**+sınıflandırma — 50 GB BAM referansla MK-09), `[[goc]]` **göç geçmişi BAŞTAN** (format 1.0.0 "İlk oluşturma"; İP-19). `disa_aktarim_icin_filtrele` = `[guvenlik]` + `gercek_yol_ipucu` çıkarılır (checksum+mantıksal yol KORUNUR). **`integrity.rs`:** BLAKE3 özet + **atomik yazma** (geçici+fsync+rename, asla yarım) + `BCP1` kendini-doğrulayan zarf (state'in `BCS1`'iyle aynı fikir; veri katmanı bağımsız). **`provenance.rs`:** `Meta` (.biocraft_meta/meta.toml) + append-only **JSONL köken günlüğü** (proje_olusturuldu/goc_uygulandi). **`export.rs`:** saf-Rust minimal **ZIP "stored"** yazıcı (CRC-32 dahil; **gerçek ZIP** — .NET ZipArchive ile doğrulandı) + küçük-dosya toplayıcı (5 MiB eşik üstü gömülmez, referansla kalır). **`mod.rs`:** `olustur` (iskele→manifest→meta→bütünlük mührü→ilk provenance; **hata/iptalde atomik temizlik** — yarım klasör YOK, kullanıcının dolu klasörünü EZMEZ), `ac` (mühür zarfı→manifest özeti→meta özeti→ayrıştır→harici-ref denetimi; çekirdek bozuksa **net Err**, harici eksik/bozuk `uyarilar`'da — **sessiz açma YOK**), `disa_aktar` (.bcproj). **app köprü (L5, MK-40):** `taslak_to_girdi` sihirbazın `ProjeTaslagi`'sını (L4) `biocraft_data::ProjeKurulumGirdisi`'ne çevirir → sihirbaz **[Oluştur]** artık **gerçek proje kurar** + son-projelere ekler + motora geçer; **başarısızlıkta sihirbaz açık kalır** (kullanıcı düzeltir). **Yeni dış bağımlılık YOK** (toml/serde/serde_json/blake3/chrono workspace'te zaten vardı). **429 test** (data 0→**22**). Demo: `cargo run -p biocraft-data --example proje_demo` (kur→aç→boz→export canlı; `.bcproj` .NET ile geçerli ZIP doğrulandı). (2026-06-21)
- **Önceki gün (16):** İP-02 **Proje Sihirbazı (1. kısım) — çok adımlı yeni-proje akışı (MK-42).** Yeni `biocraft-ui/wizard/` modülü (`steps.rs` + `mod.rs` + `tests.rs`). **`steps.rs` = saf model (egui'siz, test-edilebilir):** 6 adım tipi `SihirbazAdim` (Şablon/Bilgi/Veri/Gizlilik/Dağıtık/Özet) + indeks/sonraki/önceki gezinme; `ProjeSablonu` (Genomik/Proteomik/CRISPR/Boş — ön-kurulum **niyeti**); `VeriAyarlari` (yerel↔bağlantılı + referans↔gömülü + akış modu); **`GizlilikGuvenlik`** (`siniflandirma: Option<DataClassification>` — **None ise adım GEÇERSİZ**, MK-42); `DogrulamaHatasi` **tipli** hata enum'u (mesaj i18n view'da, mantık saf); saf yardımcılar `ad_gecerli` (yasak dosya karakteri) / `orcid_gecerli` (regex'siz, X kontrol hanesi) / `etiketleri_ayristir` (trim+dedup); `ProjeTaslagi` (Gün 17 `biocraft-data`'nın tüketeceği taslak) + `SihirbazSonucu` (Olustur/Iptal/EklentiIndir) + `SihirbazBaglam` (akıllı varsayılan girdisi). **`mod.rs` = `ProjeSihirbazi`:** durum + **adım-başına doğrulama** (`adim_hatalari`/`ileri_aktif`/`tumden_gecerli`; Özet tüm önceki adımları dener) + Geri/İleri gezinme (**geçersizken İleri pasif**) + `taslak_uret` + egui **tam-ekran** görünüm (üst: başlık + ilerleme çubuğu + adım noktaları ✓; alt: İptal · Geri · İleri/Oluştur; merkez: adım içeriği + **anlık doğrulama uyarısı**; Esc = temiz iptal). **Veri sınıflandırma ZORUNLU** (Normal / Hassas-PHI / Sentetik — seçilmeden Özet/Oluştur engellenir); **PHI seçilince güvenli kilit** = tamamen-yerel + şifreleme **zorunlu açık**, AI havuzu **zorunlu kapalı** (MK-42; view bunları pasifler). **Gizlilik varsayılanları:** yerel + AI havuzu **Hayır** + **şifreli-yerel**. **Dağıtık ağ:** eklenti kuruluysa onay kutusu, **kurulu DEĞİLSE "[İndir]" yönlendirme kartı** (İP-15 ile tutarlı). **Akıllı varsayılan:** düşük RAM → **akış modu açık** (MK-09). i18n'e 3 yeniden-kullanılır anahtar (Geri/İleri/Oluştur). **app entegrasyonu:** launcher **[Yeni Proje]** → `Sahne.sihirbaz: Option<ProjeSihirbazi>` tam-ekran açılır; Oluştur → taslak loglanır + motora geç, İptal → **temiz** launcher'a dön (sihirbaz **dosya sistemine dokunmaz** → kalıntı yok), İndir → log. **Proje dosya formatı (`biocraft.toml`/BLAKE3/export, `biocraft-data`) → Gün 17.** **Yeni dış bağımlılık YOK** (egui + `DataClassification` + i18n/token yeniden kullanıldı). **407 test** (ui 90; +25 sihirbaz). Çalışma-zamanı `--cpu`: app açıldı (**panik yok**), durum klasörü kuruldu. (2026-06-21)
- **Önceki gün (15):** İP-01 **Launcher (Epic-benzeri açılış istemcisi) — Faz 2 başladı (MK-05).** Yeni `biocraft-launcher` (L4) 6 modül (recent/news/hardware_check/launch/splash/view): son projeler (pin/arama/taşınmış-proje "yeniden bağla" madde 19/serde kalıcı), **asenkron haber akışı** (ayrı thread + `mpsc::try_recv` → **arayüz HİÇ donmaz** MK-07; yüklenirken iskelet, gelince dolar; çevrimdışı önbellek + durum göstergesi), donanım ön-kontrolü + **yetenek matrisi** (İP-08 `DonanimProfili` yeniden kullanılır; referans altıysa uyarı ama kullanıcı **DIŞLANMAZ** — MK-05), splash (E8: max 2 sn, bloklamaz, `--no-splash`, tıkla-kapat), motora geçiş (`BaslatmaArgumanlari::argv`). egui `view` İP-16 bileşenlerini + token + i18n'i **yeniden kullanır** (MK-53; dış bağlantı **onay diyaloğu**). `lib`: `DosyaDepo` atomik+BLAKE3 kalıcılık yardımcıları. **app:** `AppMod` Acilis/Motor; redraw launcher↔motor dağıtımı; **`kareyi_sun`** ortak GPU sunumu (ciz+ciz_acilis); `--skip-launcher`/`--seed-recent`. **Yeni dış bağımlılık YOK** (serde/serde_json/chrono/log zaten vardı; biocraft-ui L4↔L4 yeniden kullanıldı). **382 test** (launcher 33). Çalışma-zamanı: launcher açıldı (panik yok), haber thread'i önbelleği yazdı (boru hattı uçtan uca). (2026-06-21)
- **Önceki gün (14):** İP-07 **Eklenti Host'u (2. kısım) — İP-07 ARTIK TAM; Faz 1 bitti.** 6 yeni `biocraft-plugin-host` modülü + 1 runtime alt-modülü. **`signature.rs`** (MK-16: **Ed25519 imza** + BLAKE3 bütünlük, hash-then-sign; `GuvenDeposu` resmi/doğrulanmış yayıncı anahtarları; `ImzaDurumu` Resmi/Dogrulanmis/ImzaliBilinmeyen/Imzasiz/Gecersiz + `Rozet`; `ImzaPolitikasi` SadeceResmi/GuvenilirGerekli/ImzasizaIzinVer — **geçersiz imza her politikada reddedilir**). **`ui_ext.rs`** (UI uzantı **kayıt defteri**: panel/sekme/menü/komut/ayar + node; **çakışma yönetimi** = aynı (tür,kimlik) yuvasını iki eklenti isterse **sessiz bozmaz** → ad-uzaylı tutulur + öncelik/kullanıcı-sırası + gizle; `cakismalar()` raporu). **`isolate.rs`** (MK-15 **çökme izolasyonu**: `IzolasyonYoneticisi` çökme-bildir→yalıt→yeniden-başlat; **çökme döngüsü** MAKS_COKME=3 sonrası `KaliciHata` (artık yeniden başlatma sunulmaz); eklenti-başına `KaynakKullanim` CPU/RAM/GPU şeffaflığı). **`install.rs`** (**`.bcext`** çevrimdışı paket biçimi `BCEXT1` — bağımlılıksız uzunluk-önekli arşiv + imza fragmanı; `Kurucu` kur/güncelle/kaldır; **güncelleme geri alınabilir** yedek→yaz→hata olursa geri-sar; **aktif eklenti güncellemesi `.beklemede`'ye ertelenir**, güvenli anda uygulanır; **kaldırınca ayarları koru/sil** varsayılan koru; yol-kaçışı VFS ile korunur). **`safe_mode.rs`** (**güvenli mod**: 3. parti eklentiler kapalı başlatma, yalnızca resmi yüklenir). **`runtime/subprocess.rs`** (MK-02 **Python out-of-process köprüsü**: AYRI süreç + satır-tabanlı JSON; 2 GB/%50 CPU/30 s limitleri; **zaman aşımı gerçekten uygulanır** (süreç öldürülür); `python_bul` PATH tarama — **yoksa in-process'e DÖNMEZ**, kur rehberi). **lib.rs:** `EklentiHost` artık katmana göre dağıtır (WASM sandbox **veya** ayrı süreç; `YuklenmisEklenti.calistirici` = `Box<dyn EklentiCalistirici>`) + yüklemede imza/politika kapısı + `imza_durumu` rozeti; `guven_deposu_ile`/`imza_politikasi_ile` kurucular. **SDK `UiUzantiTuru`** `Ord` türetir. **Örnek Python eklentisi** `ornek-python/` (katman=python + eklenti.py protokol) + kapsamlı demo `eklenti_host_demo` (6 özellik canlı). **Yeni bağımlılık:** ed25519-dalek 2 (default-features=false, BSD-3-Clause, C derleyici yok) + serde_json (host). **6 kabul kriterinin tamamı karşılandı.** (2026-06-21)
- **Önceki gün (13):** İP-07 **Eklenti Host'u (1. kısım)** (MK-12/MK-13/MK-17/MK-18). Eklenti mimarisinin **kalbi** kuruldu: **gerçek Wasmtime WASM sandbox** + manifest keşfi/doğrulama + capability denetimi + VFS. **3 crate dolduruldu.** **`biocraft-sdk` (kontrat):** ABI ad alanı/sürümü + `EklentiKatmani` (MK-12 tier'leri) + capability ad çözümü (`fs`↔`Capability::Fs`) + `ui/node/data` uzantı **veri kontratları** (gösterim Gün 14). **`biocraft-ipc`:** sandbox sınırını geçen `EklentiCagrisi`/`EklentiYaniti` zarfı (`Basari`/`YetkiReddi`/`Hata`). **`biocraft-plugin-host` modülleri:** `manifest.rs` (biocraft.toml şeması + `EklentiKimligi` `biocraft.<yayinci>.<eklenti>` doğrulama + ABI/SemVer + çekirdek min/max uyumluluk → uyumsuz **net hata**), `capability.rs` (`YetkiKumesi`: **en az yetki** = istenen ∩ onaylanan + çalışma-zamanı `denetle`), `vfs.rs` (`SanalDosyaSistemi`: köke kısıtlı handle; `..`/mutlak/sürücü-ön-eki **sözlüksel reddi** → kaçış yok), `discover.rs` (klasör tarama + manifest okuma), `runtime/wasm.rs` (**Wasmtime**: bellek limiti `StoreLimits` + **CPU fuel** + capability-kapılı host fonksiyonları `gunluk_yaz`/`dosya_oku` + **AOT önbellek** serialize/deserialize), `lib.rs` (`EklentiHost` orkestratör: keşif→uyumluluk→AOT derleme→**bellek rezervasyonu** (MK-21/22 `BellekOrkestratoru`)→yükleme; `YuklenmisEklenti` RAII rezervasyon). **Örnek `biocraft.ornek.merhaba` WASM eklentisi** (elle yazılmış `merhaba.wat`, derleme adımı yok): keşfedilir/yüklenir, `merhaba()` günlüğe yazar; **fs yetkisi yokken `dosya_dene()` reddedilir, onaylanınca VFS'ten okur.** Çalıştırılabilir demo: `cargo run -p biocraft-plugin-host --example merhaba_demo`. **Yeni bağımlılık:** wasmtime 45 (kırpılmış özellik seti `cranelift/runtime/wat/std/anyhow` — **C derleyici gerekmez**) + toml 1.1. **UI uzantı/imza/izolasyon/kurulum + Tier-3 Python + Component Model → Gün 14+.** (2026-06-21)
- **Önceki gün (12):** İP-03 ana kabuk **ikinci yarısı** (editör sekmeleri + yan-yana split + detach + alt panel + inspector + özel düzen kaydet/yükle; MK-51). **3 yeni `biocraft-ui/shell/` modülü:** **`editor_area.rs`** = saf `Sekme`/`SekmeGrubu`/`EditorAlani` (ekle/kapat/yeniden-sırala/sabitle-pin + kaydedilmemiş • işareti; sabit sekmeler hep solda; kapatınca etkin komşuya kayar) + iki gruplu **yan-yana bölme** + **OS dosya sürükle-bırak (E14)** `dosya_turu`/`birakma_onizleme` (biyo-veri→Tuval, py/r→Kod, bcflow→Node; tanınmayan uzantı **reddedilir**, hedef vurgulanır/önizlenir). **`split.rs`** = saf bölme yönü (Yok/Yatay/Dikey) + `bol_boyut` rect geometrisi + oran clamp. **`bottom_panel.rs`** = `AltSekme` (Konsol/İşler/AI/Günlük) + `AltPanel`; **İşler** sekmesi İP-16 `IsIlerleme` bileşeniyle (ilerleme/iptal — Gün-4/7); AI sekmesi "yapılandırılmadı" (MK-48). **menu_bar**'a Gün-12 aksiyonları (Yeni Sekme/Alt Panel/Inspector/Editörü Böl/Yoğun-Sade Mod/Düzenleri Yönet/Bileşen Demoları) eklendi; hepsi **etkin**. **app:** editör artık **merkez** (CentralPanel); galeri Yardım→Bileşen Demoları geçişiyle merkezde açılır (CentralPanel çakışması yok); kaydedilmemiş sekme kapanırken **Gün-4 onay diyaloğu**; **Inspector** (sağ) seçili öğe özellikleri + 3B önizleme; **detach** = inspector **gerçek ikinci winit penceresine** taşınır (kendi `GpuContext`+egui bağlamı → çoklu monitör + DPI); **özel düzen yöneticisi** penceresi (adlandır→kaydet, listeden yükle/sil — `%100` sadakat); **yoğun/sade mod** boşluk ölçeğini değiştirir. **`biocraft-state` sürüm 3 göç:** `KabukDurumu`'na alt panel/inspector/bölme/yoğun-mod alanları (her biri `#[serde(default)]`) + isimli `ozel_duzenler` map; `gecerli_kil` tüm ölçüleri güvenli aralığa çeker (eski/bozuk kayıt kabuğu bozmaz). Yeni bağımlılık **yok**. (2026-06-21)
- **Sıradaki gün:** Gün 23 — **İP-06 (Kod Editörü) 2. kısım.** **node↔kod köprüsü** (`editor/bridge.rs`; Gün 21'in `python_disa_aktar`'ı + ortak workspace değişkenleri — "bu node'u kod aç" / "bu kodu node ekle") + Python öncelikli **temel** LSP (out-of-process, pyright/jedi; `plugin-host/exec/lsp.rs`) + **izole ortam** (her proje kendi sanal ortamı; arayüzden paket arama/kurma + sürüm kilidi) + kaydet'te ruff/black biçimlendirme. **İP-06 KULLANIMI (Gün 22):** kod çalıştır `biocraft_plugin_host::exec::calistir_baslat(kod, CalismaModu::TamScript|Hucre, KodCalismaLimitleri)→CalismaTutamac` (`dene`/`tumunu_dene` bloklamadan olay; `durdur` kill); arayüz tarafı `biocraft_ui::CalistirmaDurumu` (`baslat`/`yokla`/`durdur`); editör `KodEditoru::{yeni, ornek, dosya_ac, calistir_tam, calistir_hucre, ciz, yokla}`; vurgulama `editor::syntax::{BasitVurgulayici, VurgulamaOnbellek, KodDili}`; büyük dosya `editor::AkisGoruntuleyici::ac`; menü `KabukAksiyon::KodEditoru`. Spec: docs/specs/Temel-Uygulama.md → İP-06. **Ertelenenler (İP-06 sonrası):** `ropey` rope + tam özel render (çok büyük **düzenlenebilir** dosya); tam akıllı LSP + diğer diller; debugger (breakpoint/DAP); Kod→Node ters yön — hepsi v1.x. **İP-05 KULLANIMI (Gün 21):** çalıştır `biocraft_ui::node::calistir(graf, kayit, parametreler, orkestrator, &mut onbellek, &ayar, &iptal, &mut ilerleme)→CalismaSonucu`; eklenti node kaydı `YurutucuKayit::kaydet(NodeKaydi::yeni(NodeTanimi, Arc<dyn NodeCalistirici>))`; önbellek `SonucOnbellek`; kayıt `bcflow_kaydet`/`bcflow_yukle`; görsel `svg_disa_aktar`/`png_disa_aktar`; kod `python_disa_aktar`; tuval API `NodeTuvali::{calistir_baslat, calistir_simdi, node_kaydet, bcflow_metni, svg_metni, png_baytlari, python_metni}`. **Ertelenenler (İP-05 sonrası — Gün 22+):** node parametre düzenleme paneli (şema→form, anlık doğrulama); PNG sıkıştırma (stored→gerçek deflate); `rfd` ile gerçek dosya kaydet/aç (şimdilik panoya kopya); node gruplama/subgraph. **Ertelenenler (İP-09):** opsiyonel parola KDF (Argon2); native OS süreç-limiti (Job Object/cgroup); gerçek auto-updater indirme+TLS (İP-18); fuzz CI job'u (nightly). İP-10 UI ertelenenleri sürüyor: köken gezgini egui paneli + gizlilik ayar bölümü (İP-12) + onay diyaloğu. Önceki ertelenenler: `rfd` dosya seçici + gerçek HTTP/RSS haber + tarayıcı açma + `.bcproj` içe-aktarım (İP-19) + determinizm proje-ayar UI.
- **Derleme durumu:** ✅ `cargo build --workspace` (exit 0) + **612 test** geçiyor (types 29 + sdk 11 + ipc 3 + ui **189** + render 50 + mem 63 + state 50 + plugin-host **96** + launcher 33 + data 88); fmt/clippy(-D warnings)/machete/topology/**cargo-deny (exit 0)** temiz (yeni dış bağımlılık YOK — memmap2 zaten ağaçta; ui→plugin-host iç path bağımlılığı). **+6 İP-06 exec testi (Gün 22, plugin-host, Python CANLI):** kod ayrı süreçte print akar+Bitti(0); **sonsuz döngü `calistir_baslat` ANINDA döner (bloklamaz) + `durdur()`→Durduruldu** (kritik kabul kriteri); **zaman aşımı→ZamanAsimi (süreç kill)**; hatalı kod çıkış kodu 3; geçersiz yorumlayıcı→Hata (in-process'e DÖNMEZ); limit varsayılanları. **+35 İP-06 ui testi (Gün 22):** metin tamponu satır/salt-okunur; **`AkisGoruntuleyici` 2500 satır doğru sayı + kaba-indeks ötesi rastgele erişim** + salt-okunur belge + **boş dosya panik-yok** + küçük=düzenlenebilir/büyük=akış; `hucre_bul` imleçten doğru `# %%` hücresi + işaretsiz tüm metin; editör sekme aç/kapat (son sekme boş kalır) + aynı dosyayı tekrar açmaz; vurgu Python anahtar/dize/yorum/sayı + **artımlı yalnız değişen satırı yeniden hesaplar (1)** + **üç-tırnak alt-satırları tetikler** + JSON yorumsuz + Türkçe tanımlayıcı bölünmez + boş satır panik-yok + token anahtarı anlamsal (MK-52); `layout_job` tüm satırı kapsar; **headless egui** çizim 2 dil + **akış belge çizimi panik-yok**; **uçtan uca gerçek Python `print` → çıktı toplanır (donmadan)**. **Yeni dış bağımlılık YOK** (std `process`/`thread`/`mpsc` + memmap2 + saf-Rust vurgulama). Çalışma-zamanı: Python mevcut → exec uçtan uca **canlı geçti** (sonsuz döngü dondurmaz/Durdur çalışır; zaman aşımı kill; UTF-8 Türkçe çıktı). **+34 İP-05 2. kısım testi (Gün 21):** SDK kontrat (NodeTanimi serde+eski-uyum, parametre imzası kararlı+ondalık-bit-tabanlı, varsayılanlardan, NodeKaydi çalıştırır); önbellek (aynı-imza hit/farklı-imza miss, üzerine-yaz+geçersiz-kıl, budama); motor (doğrusal akış+topolojik, **önbellek değişmeyeni atlar/parametre alt-grafı geçersiz kılar**, **bağımsız dallar paralel eş-zamanlılık≥2**, **hata o dalı durdurur bağımsız dal devam**, **bütçe aşılmaz OOM-yok+sonda rezerve=0**, devasa-node zarif hata, iptal); serialize (.bcflow gidiş-dönüş+parametre korunur, **daha-yeni-sürüm reddi MK-59**, bozuk-net-hata, SVG temel öğeler, **PNG geçerli imza**, boş-graf dışa aktarılır, **CRC-32/Adler-32 bilinen değer**); kod (Python yapı+kwargs+Türkçe-temizle); canvas (senkron çalıştırma durumu uygular, 2. çalıştırma önbellekten, **arka plan çalıştırma tamamlanır**, **eklenti SDK node kaydı çalışır**, .bcflow tuval üzerinden, dışa aktarmalar, çalıştırma-kontrollü kare panik-yok). **Yeni dış bağımlılık YOK** (`std::thread::scope` paralellik + serde/serde_json + saf-Rust PNG/SVG). Çalışma-zamanı: `cargo run -p biocraft-ui --example node_calistir_demo` uçtan uca (paralel+bütçe+önbellek+.bcflow+PNG/SVG/Python+eklenti SDK node'u). **+34 node testi (İP-05 1. kısım, Gün 20):** tip uyumu (aynı tür/joker bağlanır, uyumsuz reddedilir+dönüştürücü önerisi) + yön/aynı-node hatası + **giriş tekildir** + **döngü reddi (DAG korunur)** + topolojik sıra (DAG'de var, döngüde None) + DFS yol bulma; undo/redo uçtan uca (ekle/bağla/taşı/sil + node silince kenar da gider/geri yüklenir + yinele kararlı kimlik) + tüm komutlar aynı depo (MK-37); katalog dolu/aranabilir/örnekle; tuval `tumunu_sigdir` boş→varsayılan / dolu→içeriği kapsar; **headless egui** çizim 2 dil × 3 tema + LOD/boş + palet-açık + araç undo/redo **panik yok**. **Yeni dış bağımlılık YOK** (egui + biocraft-state undo motoru + std yeniden kullanıldı). Çalışma-zamanı: Görünüm→Node Editörü merkez tuvali açar (demo genomik akış). **+38 güvenlik testi (İP-09):** AES-256-GCM şifrele/çöz gidiş-dönüş + yanlış-anahtar reddi + **kurcalanmış şifreli metin reddi** (GCM bütünlüğü) + **her çağrı taze nonce** (tekrar yok) + düz-bayt zarf gidiş-dönüş; OS-depo ikilisi sakla/al/sil + ikili anahtar hex korunur + bozuk hex reddi; **şeffaf akış** anahtar OS-deposundan kalıcı + şifrele-çöz uçtan uca; güvenli silme dosyayı kaldırır/içeriği sıfırlar/proje-değilse(N/A burada dosya) + olmayan-dosya hata; imzalı güncelleme **sahte paket reddi (checksum)** + **sahte anahtar reddi (imza)** + kurcalanmış bildirim reddi + bozuk-uzunluk net hata; log temizleme e-posta/yol/uzun-rakam maskeler + normal metin korunur + boşluk yapısı korunur; sandbox sertleştirme **zip-bomb oran reddi** + devasa girdi reddi + **çıktı u64 taşması panik-değil-hata** + çok-girdi reddi + çekirdek-arg doğrulama (boş/uzun/NUL/denetim). **Yeni dış bağımlılık:** aes-gcm/getrandom/zeroize/keyring (saf Rust, C derleyici yok) + ed25519-dalek biocraft-data'ya. Çalışma-zamanı: `cargo run --example guvenlik_demo` + `eklenti_host_demo [7]` uçtan uca. **+36 gizlilik testi (İP-10):** PHI 5 dış kanalda engellenir (sınır delinmez) + Normal/Sentetik sınıf izinli + fail-closed boş küme PHI sayılır + yerel-varsayılan profil dış gönderimi durdurur + proje profili global'i ezer (her iki yön) + PHI kanal açık olsa bile engel (üç kapı) + onay defteri kayıt/oku + köken lisans/atıf korur + köken gezgini satır/ara/atıf + k-anonimlik tekil yakalar + diferansiyel-say deterministik + tam ihraç manifest filtresiz + güvenli silme proje kaldırır/proje-değilse reddeder + envanter listeler. **Yeni dış bağımlılık YOK.** Çalışma-zamanı: `cargo run --example gizlilik_demo` uçtan uca (PHI engel + onay + köken + ihraç/silme). **NOT:** Gün 18 öncesi var olan `proje_demo.rs` örneğinde yeni clippy sürümü `only_used_in_recursion` uyarısı verdi → kullanılmayan `kok` parametresi kaldırılarak temizlendi (davranış değişmedi). **Yeni dış bağımlılık YOK** (toml/serde/serde_json/blake3/chrono workspace.dependencies'te zaten vardı; `biocraft-data`'ya bağlandı). **+22 data test:** zarf gidiş-dönüş + bozulma yakalama; içerik özeti kararlı; iskele tüm klasörler; format sürümü; **manifest TOML gidiş-dönüş** + zorunlu alanlar (ORCID/sınıflandırma/sürüm/`[[goc]]`) + **export filtresi hassas ayarı çıkarır** (checksum korunur) + hassas-dahil korur; ilk göç 1.0.0; meta gidiş-dönüş; provenance ekle-oku; **CRC-32 bilinen değer** (0xCBF43926) + ZIP imza/EOCD + ham gömme; **olustur açık klasör+geçerli manifest**; **açılışta bütünlük geçer**; **bozuk manifest net hata**; **proje-olmayan klasör net hata**; **dolu klasör reddedilir** (kullanıcı dosyası korunur); **export hassas hariç** (küçük veri gömülür); **başarısızsa yarım klasör bırakmaz** (atomik temizlik). Çalışma-zamanı: `cargo run --example proje_demo` uçtan uca (kur→aç→boz→export); `.bcproj` **.NET ZipArchive ile geçerli ZIP** doğrulandı (5 girdi okundu). GitHub Actions: Gün 16 push sonrası izlenecek.
- **Bilinen bloke eden sorunlar:** yok

---

## Faz Haritası (gün-gün yol haritasıyla uyumlu)

- **Faz 1 — Temel + Kabuk:** İP-00, İP-16, İP-04, İP-08, İP-11, İP-03, İP-07
- **Faz 2 — Launcher/Proje/Gizlilik/Güvenlik:** İP-01, İP-02, İP-10, İP-09
- **Faz 3 — Node/Kod/Ayar/Palet:** İP-05, İP-06, İP-12, İP-13
- **Faz 4 — AI Yüzey/Kanca/Onboarding/Pazar/Göç/Paketleme/QA:** İP-14, İP-15, İP-17, İP-18, İP-19, İP-20, İP-21
- **Faz 5 — Çekirdek Eklenti (BioCraft Studio):** ÇE-00, ÇE-01, ÇE-02, ÇE-04, ÇE-07, ÇE-09, ÇE-11, ÇE-12, ÇE-03, ÇE-05, ÇE-06, ÇE-08, ÇE-10

---

## Günlük İlerleme Tablosu

| Gün | Tarih | Faz/Sprint | Ne Yapıldı | Durum | Test | Sonraki |
| --- | --- | --- | --- | --- | --- | --- |
| 0 | 2026-06-20 | Pre-Sprint | Git init + GitHub remote bağlama + iskelet dosyalar (.gitignore, rust-toolchain.toml, README.md) + anayasa (ARCHITECTURE/CLAUDE/PROGRESS) + 5 spec dosyası yerleştirildi | ✅ | — | Gün 1: İP-00 Cargo Workspace |
| 1 | 2026-06-20 | Faz 1 / İP-00 | Cargo workspace (resolver=2) + biocraft-types L0 crate: ProjectId, PluginId, Version(SemVer), DataClassification(MK-42), Capability(MK-13), JobStatus, Blake3Hash, Timestamp — Türkçe döküman yorumları + 18 birim testi | ✅ | 18/18 geçti | Gün 2: İP-16 (TDA hata şeması) veya diğer İP-00 parçası (iskelet crate'ler + CI) |
| 2 | 2026-06-20 | Faz 1 / İP-00 | 12 stub crate iskelet (L1–L5): biocraft-sdk/ipc/data/state/mem/render/plugin-host/net/ai-surface/ui/launcher/app — kök Cargo.toml 13 üye, hepsini derliyor; cargo-machete (0 kullanılmayan dep); MK-40 topoloji kontrol scripti (scripts/check-topology.py, Python+cargo metadata); .github/workflows/ci.yml (build/test/fmt/clippy/audit/machete/topology); fmt+clippy temiz | ✅ | 18/18 geçti (stubs sıfır test, types geçiyor) | Gün 3: CI hattı genişletme |
| 3 | 2026-06-20 | Faz 1 / İP-00 | CI hattı genişletme (MK-58/MK-60): Windows+Linux matrix; cargo-deny + deny.toml (lisans politikası + advisory — Hukuk-ve-Operasyon §1); rustfmt.toml + clippy.toml; ayrı `deny` job'u. **Canlı CI'da 3 sorun çıktı→çözüldü:** (1) Windows'ta topoloji scripti UnicodeEncodeError → `sys.stdout.reconfigure(utf-8)`; (2) deny.toml deprecated anahtarlar (unlicensed/allow-osi-fsf-free) silindi; (3) 13 crate'e `license.workspace=true`+`publish=false`, deny.toml `allow-wildcard-paths=true`. **Doğrulama testleri:** Actions'ta workflow çalıştı✅, tüm adımlar yeşil✅, bilerek format hatası→CI kırmızı (fmt adımı fail) doğrulandı✅ (feature dalında, sonra silindi) | ✅ | 18/18; fmt/clippy/topology/cargo-deny temiz; 3 job yeşil (canlı) | Gün 4: İP-16 (TDA hata şeması — `biocraft-types` içine `BioCraftError`, standart hata yapısı, `correlation_id`) |
| 4 | 2026-06-20 | Faz 1 / İP-16 | Ortak TDA (3. derece) arayüz bileşenleri (MK-53), `biocraft-ui` altında egui ile, "bir kez yaz her yerde kullan": **8 bileşen** (toast/bildirim, hata diyaloğu, boş durum, yükleme iskeleti, onay diyaloğu, büyük işlem tahmini, ilerleme/iş, durum rozetleri) + **örnek galeri ekranı** (tema açık/koyu + dil TR/EN değiştirici). Ek modüller: `tokens` (tema-duyarlı tasarım token'ları) + `i18n` (EN/TR, anahtar enum'u → eksik çeviri derlenmez). `biocraft-types`'a **standart hata şeması**: `ErrorReport` (ne/neden/çözüm tip düzeyinde zorunlu) + `CorrelationId`. egui 0.29 bağımlılığı; egui'nin gömülü fontları için deny.toml'a OFL-1.1 + UFL-1.0 muafiyeti. Zaman/iptal/throttle mantığı egui'siz birim-testlenebilir tutuldu; her diyalog headless egui karesiyle test edildi | ✅ | 51/51 (types 24 + ui 27); fmt/clippy(-D warnings)/machete/topology/cargo-deny **temiz** (yerelde) + CI'da 3 job yeşil (canlı) | Gün 5: İP-04 (winit+wgpu+egui pencere; örnek galeriyi canlı göster) |
| 5 | 2026-06-20 | Faz 1 / İP-04 | **Render ve tuval altyapısı temeli.** `biocraft-render` (L3) saf/test-edilebilir motor: `frame_budget` (~16 ms kare bütçesi, FPS, Eco mod, GPU ≤100 ms batch — MK-03/MK-04), `tdr` (DeviceLost kurtarma durum makinesi, <5 sn hedef, tekrarlı çökmede CPU'ya düş — MK-04), `backend` (tek aktif backend seçimi: wgpu/CUDA-iskelet/CPU), `gpu` (wgpu cihaz/kuyruk/yüzey, winit penceresi, CPU fallback = WARP fallback adapter). `biocraft-app` (L5) pencere host'u: winit 0.30 olay döngüsü + egui↔wgpu köprüsü → **örnek TDA galerisi canlı pencerede**; alt durum çubuğunda FPS + backend + CPU uyarısı; **T tuşu = TDR simülasyonu** ("GPU yeniden başlatıldı" bildirimi). MK-40: render egui'ye bağlı değil (köprü host'ta). Sürümler workspace'te sabit (wgpu 22/winit 0.30/egui 0.29). **Çalışma-zamanı doğrulandı:** gerçek GPU (RTX 4060 Ti) + `--cpu` WARP fallback çökmeden başladı; egui için doğrusal `Bgra8Unorm` format (renk doğruluğu) | ✅ | 70/70 (types 24 + ui 27 + render 19); fmt/clippy(-D warnings)/machete/topology temiz; cargo-deny exit 0 (14 zararsız duplicate uyarısı) | Gün 6: İP-08 (Bellek Orkestratörü + Donanım Koruma) |
| 6 | 2026-06-20 | Faz 1 / İP-04 | **Render tamamlama + tasarım sistemi (MK-52, MK-04).** (1) **Token sistemi:** `assets/tokens.json` (tek renk kaynağı) → `biocraft-render/tokens.rs` saf token motoru (Renk RGBA8 + hex, Palet, Tema, **TokenDeposu**: tema değiştir O(1) <100 ms, **özel tema oluştur/JSON kaydet/geri yükle = E2**). UI tarafı `biocraft-ui/tokens.rs` ince adaptör (Renk→egui Color32, `egui_visuals()` ile TÜM egui token'dan). **Kodda sabit renk kalmadı** (named sabitler + 4 bileşendeki `Color32::WHITE` + app clear/durum renkleri token'a taşındı; grep sıfır). (2) **Tipografi:** `tipografi.rs` (Inter/JetBrains Mono/Space Grotesk rolleri + boyut + DPI ölçek); egui adaptörü font yükler, yoksa egui gömülü fontuna düşer (TDA m.1, loglu). (3) **2B plot:** `plot.rs` saf model (çizgi/scatter + veri→ekran + **culling + LOD seyreltme**) → egui `PlotWidget`; galeride coverage+varyant demosu. (4) **3B sahne:** `scene3d.rs` saf geometri/kamera (küre/silindir/top-çubuk, look-at+perspektif MVP) + **native wgpu çizici** (off-screen renk+derinlik, Lambert shader, malzeme=token) → egui'ye doku olarak; sağ panelde canlı dönen top-çubuk. (5) **LOD:** `lod.rs` görünür-alan culling + LOD kademe + seyreltme. 3 tema galeride döngüyle değişiyor. **Çalışma-zamanı:** `--cpu` WARP'ta WGSL derlendi + 3B pipeline + egui doku entegrasyonu çökmeden çalıştı (gerçek GPU görsel doğrulaması kullanıcıda) | ✅ | 108/108 (types 24 + ui 34 + render 50); fmt/clippy(-D)/machete/topology/cargo-deny temiz; serde+serde_json+bytemuck eklendi (deny ok) | Gün 7: İP-08 (Bellek Orkestratörü + Donanım Koruma) |
| 7 | 2026-06-21 | Faz 1 / İP-08 | **Global Bellek Orkestratörü (OOM koruması) + bütçe + out-of-core.** `biocraft-mem` (L2): (1) **orchestrator.rs** — `BellekOrkestratoru` (Arc<Mutex>, ucuz klon); RAII `Rezervasyon`/`OnbellekTutamac` (drop=otomatik iade); `rezerve_et` bütçe aşımında **panik DEĞİL** standart `ErrorReport` döner (MK-22); yer açmak için **LRU önbellek boşaltma**; `bellek_baskisi()` agresif temizleme; `durum()`+`bilesen_dokumu()` teşhis; çok-threadli güvenlik testi. (2) **budget.rs** — `dosya_butce_kontrol` (boyut × genişleme kat → tahmini RAM); sığmazsa **akış / cloud-burst (yer tutucu) / iptal** teklifi (4 TB'da bile "load all" yok — MK-09). (3) **priority.rs** — `OncelikModu` (Arayüz/Denge/Maksimum) → `hesap_plani` worker sayısı + **Zero-Impact** kullanıcı-aktif kısma kancası. (4) **akis.rs** — out-of-core: `akisla_isle`(Read) / `dosya_akisla_isle`(BufReader) / `mmap_ile_isle`(memmap2); yalnızca **bir pencere** rezerve → RAM'den büyük veri parça parça (test: 1 MB bütçe + 8 MB veri, tepe = 1 pencere). UI: **butce_dialog.rs** (mem teklifini Gün-4 stiliyle iki-dilli diyaloğa çevirir; egui L4) + galeride **canlı demo** (bellek çubuğu, +32 MB rezerve/önbellek, bellek baskısı, 4 TB bütçe diyaloğu, 8 MB akış, öncelik döngüsü). i18n'e 3 anahtar + `butce_metni`. `biocraft-ui→biocraft-mem` (L4→L2, topoloji temiz); memmap2 eklendi (deny ok). Donanım termal koruma Gün 8'e bırakıldı. | ✅ | 140/140 (types 24 + ui 36 + render 50 + mem 30); fmt/clippy(-D)/machete/topology/cargo-deny(exit 0) temiz; gallery İP-08 bölümü headless egui'de tüm tema/dilde çökmeden çizildi | Gün 8: İP-08 Donanım Koruma (termal eşik + watchdog + disk koruması + donanım göstergesi) |
| 8 | 2026-06-21 | Faz 1 / İP-08 | **Donanım Koruma (Zero-Impact donanım yarısı) — MK-24/25/26/29.** `biocraft-mem`'e 6 yeni modül: (1) **thermal.rs** — saf termal eşik tablosu (GPU 70/75/80/85, CPU 75/85/95, NVMe 60/70 °C → `TermalAksiyon` TamKapasite/YukAzalt(75/50)/Duraklat/AcilDurdur), histerezisli "soğuyunca otomatik devam", `en_kotu_aksiyon` (parçalar arası en ciddi). (2) **hardware_guard.rs** — `DonanimSensoru` trait (gerçek `SistemSensoru`=sysinfo / `BetikSensor`/`SabitSensor` test / `SensorYok`), bağımsız `DonanimMuhafiz` watchdog (ayrı `std::thread`, ana döngüye bağlı değil, RAII durdur+drop), `adim_uygula` saf çekirdek: duraklamaya geçişte **checkpoint** (veri kaybı yok), **sensör yoksa zarif devre dışı + bilgi (çökme yok)**. (3) **disk_guard.rs** — %10 uyarı/%2 salt-okunur+100MB marj, `dogru_surucu_mu` yanlış-sürücü koruması (Win sürücü harfi/Unix kök), `disk_durumu_oku` sysinfo sürücü-başına. (4) **autotune.rs** — `DonanimProfili`→`DonanimSinifi`(Düşük/Orta/Yüksek)→`OtoAyar` (Eco/Bio + düşük donanımda sadeleşme+uyarı+30 FPS), `asgari_emulasyon` (--emulate-min). (5) **determinism.rs** — `DeterminizmBayragi` kancası (HizliKesif/TekrarUretilebilir + sabit tohum/sıralı indirgeme/worker kısıdı; bit-bit garanti v1.x). (6) **metrics.rs** — `TepeOrtalama` (tepe/ortalama + regresyon eşiği). App: `--emulate-min`, watchdog (sysinfo+simülasyon), durum çubuğunda donanım göstergesi + "soğutuluyor" rozeti, 'I'/'O' ısı simülasyon tuşları. **NVML gerçek entegrasyonu insan-eli/sürücü işi → kanca.** | ✅ | 173/173 (types 24 + ui 36 + render 50 + **mem 63**); fmt/clippy(-D)/machete/topology/cargo-deny(exit 0) temiz; sysinfo 0.33 eklendi (MIT, deny ok). 4 kabul senaryosu test edildi: ısınma→kademeli→acil+checkpoint, sensör-yok zarif kapanış, disk %2 salt-okunur, --emulate-min sadeleşme | Gün 9: İP-11 (Geri Al/Yinele) |
| 9 | 2026-06-21 | Faz 1 / İP-11 | **Durum + Otomatik Kayıt + Çökme Kurtarma (self-healing — MK-28/37/38).** Yeni **`biocraft-state` (L2)** çekirdeği 4 modül: (1) **store.rs** — atomik-dosya `KaliciDepo` (geçici dosya + `fsync` + atomik `rename`; yarım yazma yok) + **BLAKE3 bütünlük zarfı** (`BCS1 <hex>\n<yük>`; okumada doğrulanır, bozulma `Err`); tek mantıksal depo (MK-37, "çok-depoda atomiklik" VAAT YOK). (2) **state.rs** — `UygulamaDurumu` (pencere boyutu/maksimize + tema + dil + panel boyutu + açık sekmeler + tercihler; serde JSON + sürüm/göç). (3) **autosave.rs** — saf `OtomatikKayit` (30 sn periyodik + 2 sn debounce; sahte saatle test). (4) **recovery.rs** — temiz-kapanış bayrağı → çökme tespiti ("kurtarılan oturum"; temiz kapanışta sunulmaz). **lib.rs** `DurumYoneticisi` orkestratör (yükle→eşitle→belki_kaydet→temiz_kapat; bozuk durumda güvenli varsayılana **düşer** — MK-28 kural 2). App: tema/dil/pencere/panel geri yüklenir + otomatik kaydedilir; Esc/kapat=temiz kapanış; çökme sonrası üstte "Kurtarılan oturum" bandı. **blake3 1.x** (`pure`, CC0/Apache, deny ok). **Undo/Redo Gün 10'a bırakıldı.** | ✅ | 194/194 (types 24 + ui 36 + render 50 + mem 63 + **state 21**); fmt/clippy(-D)/machete/topology/cargo-deny(exit 0) temiz. Çalışma-zamanı: gerçek GPU'da açıldı, `.bcs` durum dosyaları yazıldı, zorla kapatma→sonraki açılışta kurtarma tespiti logda doğrulandı | Gün 10: İP-11 Undo/Redo (Command Pattern) |
| 10 | 2026-06-21 | Faz 1 / İP-11 | **Geri Al/Yinele (Command Pattern + ters-işlem) + çakışma tespiti + yerel geçmiş — MK-36/MK-37.** `biocraft-state`'e 5 modül: **command.rs** (`Komut<H>` trait: uygula/geri_al/aciklama/`depo()` + `DepoKimligi` + `BirlesikKomut` = aynı depo grubu, farklı depo **reddedilir**, alt-komut hatasında atomik geri sarma), **undo.rs** (`GeriAlYigini<H>` çok-adımlı; yeni iş redo'yu temizler; derinlik sınırı; geri-al/yinele etiketleri), **conflict.rs** (`CakismaIzleyici`: BLAKE3 taban vs disk → `GuvenliYaz`/`Cakisma`; `CozumSecimi` Bizimkini/Diski/İptal — **sessiz ezme YOK**), **history.rs** (`YerelGecmis`: zaman damgalı anlık görüntüler; aynı içerik tekrar elenir; `geri_yukle`), **durum_komutlari.rs** (`UygulamaDurumu` örnek komutları: Tema/Dil/Panel simetrik + Sekme ekle/kapat yakalayan). App'te **sol panel canlı demo** (işlemler + ↶/↷ + komut geçmişi + yerel geçmiş + çakışma çözümü). Motor **genel** — node/kod/ayar paketleri aynısını kullanacak. **chrono 0.4** eklendi (deny ok). | ✅ | 219/219 (types 24 + ui 36 + render 50 + mem 63 + **state 46**); fmt/clippy(-D)/machete/topology/cargo-deny(exit 0) temiz; 4 kabul senaryosu testli: çok-adımlı undo/redo, tek-depo MK-37 (BirlesikKomut farklı depoyu reddeder), çakışma uyarısı+sürüm seçimi, zaman damgalı snapshot listesi. Çalışma-zamanı `--cpu` panik yok | Gün 11: İP-03 (6-bölge ana pencere kabuğu) |
| 11 | 2026-06-21 | Faz 1 / İP-03 | **Ana kabuk — 6-bölge düzeninin dört bölgesi + klasik menü (MK-51, MK-52).** Yeni `biocraft-ui/shell/` (7 dosya): **layout** (saf bölge ölçüleri Title 32/Activity 48/Status 22/Side 200–600 px + `yan_panel_sikistir` aralık-zorlama, NaN→varsayılan), **activity_bar** (`ActivityMod` Proje/Eklentiler/Arama/AI/Veritabanı/Ayar; 48px sol şerit; aktif mod vurgu renginde; L4↔L2 `AktifModSecimi` eşlemesi), **menu_bar** (`KabukAksiyon` **tek komut tanımı** — Dosya/Düzen/Görünüm/Eklenti/Yardım; menü+palet aynı enum'a bağlanır, çakışma yok; ilgili paketi olmayan aksiyon **devre dışı**, sahte "çalışıyor" yok), **title_bar** (32px üst: marka + klasik menü + komut paleti tetikleyici + tema/dil hızlı eylem), **side_panel** (200–600px yeniden boyutlanır; içerik moda göre: proje ağacı yer tutucu/"eklenti yok"/arama/**AI yapılandırılmadı MK-48**/DB yok/ayar — eksikler açık etiketli TDA m.1), **status_bar** (22px: FPS+backend+İP-08 donanım+**bağlantı rozeti**+**token sayacı(—)**+**aktif iş**; `DurumBilgisi` struct). `biocraft-state` **sürüm 2 göç**: `KabukDurumu` (aktif_mod+yan_panel_acik+yan_panel_genislik) `#[serde(default)]` (eski kayıt uyumlu). App `ciz()` kabuğu kompone eder; seçili mod + panel genişliği **oturumlar arası kalıcı**; menü Çıkış temiz kapatır; Gün-10 undo/redo demosu yüzen pencereye taşındı. Bağımlılık eklenmedi. | ✅ | 230/230 (types 24 + **ui 46** + render 50 + mem 63 + **state 47**); fmt/clippy(-D)/machete/topology/cargo-deny(exit 0) temiz. Çalışma-zamanı `--cpu` panik yok; `surum:2`+`kabuk{}` diske yazıldı (1→2 göç doğrulandı) | Gün 12: İP-03 ikinci yarısı (Editor sekmeleri+split + alt Panel) |
| 12 | 2026-06-21 | Faz 1 / İP-03 | **Ana kabuk ikinci yarısı — editör sekmeleri + split + detach + alt panel + inspector + özel düzen (MK-51).** 3 yeni `biocraft-ui/shell/` modülü: **editor_area** (saf `Sekme`/`SekmeGrubu`/`EditorAlani`: ekle/kapat-komşuya-kay/yeniden-sırala/sabitle-başta + kaydedilmemiş • + iki gruplu **yan-yana bölme** + **E14 OS sürükle-bırak** `dosya_turu`/`birakma_onizleme`: biyo-veri→Tuval, py/r→Kod, bcflow→Node, tanınmayan **reddedilir**+önizlenir), **split** (Yok/Yatay/Dikey + `bol_boyut` rect + oran clamp), **bottom_panel** (Konsol/İşler/AI/Günlük; İşler=İP-16 `IsIlerleme` ilerleme/iptal; AI "yapılandırılmadı" MK-48). **menu_bar**: Yeni Sekme/Alt Panel/Inspector/Editörü Böl/Yoğun-Sade/Düzenleri Yönet/Bileşen Demoları (hepsi etkin). **app**: editör=merkez (CentralPanel), galeri Yardım→Demolar geçişi; kaydedilmemiş sekme kapanırken **Gün-4 onay**; **Inspector**=özellikler+3B önizleme; **detach**=inspector **gerçek 2. winit penceresine** (kendi GpuContext+egui → çoklu monitör/DPI); **özel düzen yöneticisi** (adlandır→kaydet, yükle/sil %100 sadakat); **yoğun/sade mod**. `biocraft-state` **sürüm 3 göç**: KabukDurumu'na alt-panel/inspector/bölme/yoğun alanları (`serde(default)`) + `ozel_duzenler` map + `gecerli_kil` güvenli-değer clamp. Bağımlılık eklenmedi. | ✅ | 252/252 (types 24 + **ui 65** + render 50 + mem 63 + **state 50**); fmt/clippy(-D)/machete/topology/cargo-deny(exit 0) temiz. Çalışma-zamanı `--cpu` panik yok. 7 saf-mantık test bloğu: sekme ekle/kapat/sabit/sırala, dosya_turu/önizleme, split geometri, alt-panel, sürüm-3 göç + özel düzen serde | Gün 13: İP-07 (Eklenti Host'u) |
| 13 | 2026-06-21 | Faz 1 / İP-07 | **Eklenti Host'u (1. kısım) — eklenti mimarisinin kalbi: gerçek Wasmtime WASM sandbox + manifest + capability + VFS (MK-12/13/17/18).** **`biocraft-sdk` kontrat** (ABI ad alanı `biocraft`/sürüm 0.1 + `EklentiKatmani` MK-12 tier'leri + `yetenek_ayristir` fs↔Capability + `ui/node/data` uzantı veri kontratları — gösterim Gün 14). **`biocraft-ipc`**: `EklentiCagrisi`/`EklentiYaniti` (`Basari`/`YetkiReddi`/`Hata`) sandbox-sınırı zarfı. **`biocraft-plugin-host`**: `manifest.rs` (biocraft.toml + `EklentiKimligi` `biocraft.<yayinci>.<eklenti>` doğrulama + ABI/SemVer + çekirdek min/max → uyumsuz **net hata**), `capability.rs` (`YetkiKumesi` **en az yetki**=istenen∩onaylanan + çalışma-zamanı `denetle`), `vfs.rs` (`SanalDosyaSistemi` köke-kısıtlı handle; `..`/mutlak/sürücü-ön-eki **sözlüksel reddi**), `discover.rs` (tarama+manifest oku), `runtime/wasm.rs` (**Wasmtime** bellek limiti `StoreLimits` + **CPU fuel** + capability-kapılı host fonksiyonları `gunluk_yaz`/`dosya_oku` + **AOT önbellek** serialize/deserialize), `lib.rs` (`EklentiHost`: keşif→uyumluluk→AOT→**bellek rezervasyonu** MK-21/22→yükle; `YuklenmisEklenti` RAII). **Örnek `biocraft.ornek.merhaba`** (elle yazılmış `merhaba.wat`, derleme adımı yok) + `cargo run --example merhaba_demo`. **Yeni dep:** wasmtime 45 (kırpılmış: cranelift/runtime/wat/std/anyhow → C derleyici yok) + toml 1.1. `biocraft-types`: `Version` FromStr/`uyumlu_mu` + `Capability` `Ord`. **UI uzantı/imza/izolasyon/kurulum + Tier-3/Component Model → Gün 14.** | ✅ | 299/299 (types 29 + sdk 7 + ipc 3 + ui 65 + render 50 + mem 63 + state 50 + **plugin-host 32**); fmt/clippy(-D)/machete/topology/**cargo-deny exit 0 (wasmtime lisans temiz)**. Gerçek Wasmtime testleri: keşfet→yükle→merhaba✅; fs yetkisi yoksa dosya reddedilir/onaylanınca okur; bellek+fuel limiti trap; AOT 2. yükleme isabet; bütçe yetmezse yüklenmez. Demo canlı çalıştı (panik yok) | Gün 14: İP-07 2. kısım (UI uzantı/imza/izolasyon/kurulum) |
| 14 | 2026-06-21 | Faz 1 / İP-07 | **Eklenti Host'u (2. kısım) — İP-07 TAM; Faz 1 bitti (MK-15/16/17/02).** 6 yeni modül: **signature.rs** (Ed25519 imza + BLAKE3 bütünlük hash-then-sign; `GuvenDeposu` resmi/doğrulanmış; `ImzaDurumu` Resmi/Dogrulanmis/ImzaliBilinmeyen/Imzasiz/Gecersiz + `Rozet`; `ImzaPolitikasi` 3 seviye — **geçersiz imza her politikada red**), **ui_ext.rs** (UI uzantı **kayıt defteri** panel/sekme/menü/komut/ayar+node; **çakışma yönetimi** aynı (tür,kimlik) yuvası → sessiz bozmaz, ad-uzaylı + öncelik/kullanıcı-sıra/gizle + `cakismalar()`), **isolate.rs** (MK-15 `IzolasyonYoneticisi` çökme-bildir→yalıt→yeniden-başlat; döngü MAKS=3 → `KaliciHata`; eklenti-başına CPU/RAM/GPU `KaynakKullanim`), **install.rs** (`.bcext` `BCEXT1` bağımlılıksız arşiv+imza fragmanı; `Kurucu` kur/güncelle/kaldır; **güncelleme geri alınabilir** yedek→yaz→geri-sar; **aktif → `.beklemede`'ye ertelenir**; **kaldırınca ayar koru/sil** varsayılan koru; yol-kaçışı VFS korumalı), **safe_mode.rs** (güvenli mod: 3. parti kapalı, yalnız resmi), **runtime/subprocess.rs** (MK-02 Python **AYRI süreç** + JSON satır protokolü; 2GB/%50/30s; **zaman aşımı süreç öldürür**; `python_bul` PATH — **yoksa in-process'e DÖNMEZ** kur rehberi). lib.rs katman dağıtımı (`Box<dyn EklentiCalistirici>`) + imza/politika kapısı + rozet. SDK `UiUzantiTuru` `Ord`. Örnek `ornek-python/` + demo `eklenti_host_demo`. **Dep:** ed25519-dalek 2 (BSD-3, C derleyici yok) + serde_json. | ✅ | 349/349 (types 29 + sdk 7 + ipc 3 + ui 65 + render 50 + mem 63 + state 50 + **plugin-host 82**); fmt/clippy(-D)/machete/topology/**cargo-deny exit 0 (ed25519/curve25519 BSD-3 temiz)**. 6 kabul kriteri ✅: WASM+Python keşif/yükle; capability+VFS ihlali engel; çöken eklenti çekirdeği düşürmez+yeniden başlatılır; imzasız uyarı+`.bcext` çevrimdışı kurulum; panel/menü/komut/node kaydı görünür+çakışma yönetilir; güvenli mod 3. partiyi kapatır. Demo canlı (Python farklı PID) | Gün 15: Faz 2 — İP-01 Launcher (veya İP-09 sertleştirme) |
| 15 | 2026-06-21 | Faz 2 / İP-01 | **Launcher (Epic-benzeri açılış istemcisi) — Faz 2 başladı (MK-05).** Yeni **`biocraft-launcher` (L4)** 6 modül: **recent** (son projeler: pin/arama/`MAKS_PROJE` kırpma-sabit-korur/taşınmış-proje `proje_durumu`+`yeniden_bagla` **madde 19**/serde), **news** (**asenkron** haber: `HaberKaynagi` trait + `YerelKaynak` küratörlü akış + `HaberYukleyici` **ayrı thread + `mpsc::try_recv`** → **arayüz HİÇ bloklanmaz** MK-07; `HaberDurumu` Yükleniyor/Yüklendi/Çevrimdışı/Hata + önbellek/çevrimdışı), **hardware_check** (İP-08 `DonanimProfili` **yeniden kullanılır** + `ReferansDonanim` + `YetenekMatrisi` ne-yapılabilir/yapılamaz; kullanıcı **DIŞLANMAZ** bilgilendirilir — MK-05), **launch** (`BaslatmaArgumanlari::argv` motor argümanları + `LauncherEylem`), **splash** (E8: max 2 sn + `--no-splash` + tıkla-kapat; **bloklamaz**), **view** (egui: üst başlık+çevrimdışı rozeti+eylemler / sol son-projeler arama+pin+yeniden-bağla+boş-rehber / merkez haber-**iskelet**+donanım-uyarı-kartı+yetenek-matrisi+yenilikler; dış bağlantı **onay diyaloğu**). lib: kalıcılık yardımcıları (`DosyaDepo` **atomik+BLAKE3 yeniden kullanılır**). **app entegrasyonu:** `AppMod` Acilis/Motor; `--no-splash`/`--skip-launcher`/`--seed-recent`; redraw launcher↔motor dağıtımı; **`kareyi_sun`** ortak GPU sunumu (ciz+ciz_acilis); launcher eylemleri (ProjeyiBaslat→`recent.acildi`+motor; YeniProje/ProjeAc→motor). biocraft-ui (L4↔L4) bileşen/token/i18n **yeniden kullanıldı** (MK-53). **Yeni dış bağımlılık YOK.** | ✅ | 382/382 (types 29 + sdk 7 + ipc 3 + ui 65 + render 50 + mem 63 + state 50 + plugin-host 82 + **launcher 33**); fmt/clippy(-D)/machete/topology/**cargo-deny(exit 0)** temiz. Çalışma-zamanı `--cpu --seed-recent`: launcher açıldı (**panik yok**), asenkron haber thread'i `haber_onbellek.bcs` yazdı (boru hattı **uçtan uca** doğrulandı), donanım uyarısı tetiklendi; `--skip-launcher` doğrudan motor (regresyonsuz) | Gün 16: İP-02 (Proje Sihirbazı + Proje Formatı) |
| 16 | 2026-06-21 | Faz 2 / İP-02 | **Proje Sihirbazı (1. kısım) — çok adımlı yeni-proje akışı (MK-42).** Yeni `biocraft-ui/wizard/` (steps.rs + mod.rs + tests.rs). **steps.rs = saf model (egui'siz):** `SihirbazAdim` 6 adım (Şablon/Bilgi/Veri/Gizlilik/Dağıtık/Özet) + indeks/sonraki-önceki; `ProjeSablonu` (Genomik/Proteomik/CRISPR/Boş — ön-kurulum niyeti); `VeriAyarlari` (yerel↔bağlantılı + referans↔gömülü + akış); **`GizlilikGuvenlik`** (`Option<DataClassification>` — **None=adım geçersiz**, MK-42); `DogrulamaHatasi` tipli enum (mesaj i18n'de); saf yardımcı `ad_gecerli`/`orcid_gecerli` (X kontrol hanesi, regex'siz)/`etiketleri_ayristir` (trim+dedup); `ProjeTaslagi` (Gün 17 `biocraft-data` tüketir) + `SihirbazSonucu` + `SihirbazBaglam`. **mod.rs = `ProjeSihirbazi`:** adım-başına doğrulama (`adim_hatalari`/`ileri_aktif`/`tumden_gecerli`) + Geri/İleri (**geçersizde İleri pasif**) + `taslak_uret` + egui tam-ekran (üst başlık+ilerleme çubuğu+adım noktaları ✓ / alt İptal·Geri·İleri-Oluştur / merkez içerik+anlık uyarı; Esc=temiz iptal). **Sınıflandırma ZORUNLU** (Normal/Hassas-PHI/Sentetik); **PHI güvenli kilit** (yerel+şifreleme zorunlu açık, AI havuzu zorunlu kapalı — view pasifler). Varsayılan: yerel/AI-Hayır/şifreli. **Dağıtık ağ eklenti yoksa "[İndir]" kartı** (İP-15). Akıllı varsayılan: düşük RAM→akış. i18n+3 anahtar (Geri/İleri/Oluştur). **app:** launcher [Yeni Proje] → `Sahne.sihirbaz` tam-ekran; Oluştur→taslak log+motor, İptal→**temiz** launcher (dosyaya dokunmaz), İndir→log. **Proje formatı (biocraft.toml/BLAKE3/export) → Gün 17.** **Yeni dış bağımlılık YOK.** | ✅ | 407/407 (types 29 + sdk 7 + ipc 3 + **ui 90** + render 50 + mem 63 + state 50 + plugin-host 82 + launcher 33); fmt/clippy(-D)/machete/topology/**cargo-deny(exit 0)** temiz. +25 sihirbaz test: navigasyon, doğrulama, **ZORUNLU sınıflandırma** (seçilmeden ilerlenemez), PHI güvenli kilit, akıllı varsayılan, taslak üretimi, saf yardımcılar, headless egui (her adım panik yok TR+EN, Esc→İptal). Çalışma-zamanı `--cpu`: app açıldı (**panik yok**), durum klasörü kuruldu | Gün 17: İP-02 (2. kısım) Proje Formatı (biocraft.toml + klasör + BLAKE3 + .bcproj export, biocraft-data) |
| 17 | 2026-06-21 | Faz 2 / İP-02 | **Proje Formatı (2. kısım) — İP-02 TAM (MK-31/33/34/59).** `biocraft-data` (L2) dolduruldu: `project/` 6 modül. **Proje = klasör** (`biocraft.toml` + `data/inputs,intermediate` + `flows/` + `scripts/` + `provenance/` + `.biocraft_meta/`). **manifest.rs:** kök = yalnız alt-tablolar; `[kimlik]` (ad/açıklama/**BioCraft+format sürümü** MK-59/şablon/tarihler), `[olusturan]` (**ORCID**+kurum), `[siniflandirma]` (zorunlu **DataClassification** MK-42+uyumluluk+lisans+etiket), `[gizlilik]` (yerel/AI/dağıtık/**determinizm kancası**), **`[guvenlik]` HASSAS** (şifreleme — export'ta HARİÇ), `[veri]`, `[[harici_veri]]` (yol+**gerçek-yol-ipucu**+boyut+**BLAKE3**+sınıf — MK-09 referans), `[[goc]]` **göç geçmişi BAŞTAN** (1.0.0). `disa_aktarim_icin_filtrele` (guvenlik+yol-ipucu çıkar, checksum korur). **integrity.rs:** BLAKE3 + **atomik yaz** (geçici+fsync+rename) + `BCP1` zarf. **provenance.rs:** Meta + append-only **JSONL köken günlüğü**. **export.rs:** saf-Rust **ZIP "stored"** (CRC-32; gerçek ZIP) + küçük-dosya toplayıcı (5 MiB üstü referansla kalır). **mod.rs:** `olustur` (**hata/iptalde atomik temizlik**, dolu klasör EZMEZ), `ac` (mühür→manifest→meta→harici-ref; bozuk=**net Err**, harici eksik=`uyarilar`, **sessiz açma YOK**), `disa_aktar`. **app köprü (L5/MK-40):** `taslak_to_girdi` → sihirbaz **[Oluştur] gerçek proje kurar** + son-projelere ekler; başarısızlıkta sihirbaz açık kalır. **Yeni dış bağımlılık YOK.** | ✅ | 429/429 (… launcher 33 + **data 22**); fmt/clippy(-D)/machete/topology/**cargo-deny(exit 0)** temiz. +22 data test (zarf/bozulma, manifest TOML+zorunlu alanlar, **export hassas-hariç**, CRC-32 0xCBF43926+ZIP imza, **kur açık-klasör+manifest**, **açılış bütünlük geçer**, **bozuk manifest net hata**, **dolu klasör reddedilir+korur**, **başarısızsa yarım klasör yok**). Çalışma-zamanı: `--example proje_demo` uçtan uca; `.bcproj` **.NET ZipArchive geçerli ZIP** doğrulandı (5 girdi) | Gün 18: İP-10 (Veri Sınıflandırma & Gizlilik uygulaması — çekirdek çıkış kapısı, MK-42/43) |
| 18 | 2026-06-21 | Faz 2 / İP-10 | **Gizlilik & Veri Yönetimi — gizliliği ÇEKİRDEK seviyesinde uygula (MK-41/42/43/34).** Yeni `biocraft-data/src/privacy/` 7 modül. **classify.rs = ÇIKIŞ KAPISI:** `DisKanal` (P2p/DisAi/DisApi/Telemetri/Paylasim) + `cikis_denetle`→`CikisKarari` (PHI **mutlak `Engellendi`**, atlanamaz) + `en_kisitlayici` **fail-closed** (boş küme→PHI) + `kume_denetle`. **Mimari kilit:** kapı L2'de; net/ai-surface L3 ona bağımlı → MK-40 ile atlanamaz (stub'lara sözleşme notu). **profile.rs:** `GizlilikProfili` yerel-varsayılan (tamamen_yerel/telemetri kapalı/AI havuzu kapalı/her gönderim onay) + `proje_ile_coz` proje **global'i EZER** + `dis_kanal_etkin_mi`. **consent.rs:** ÜÇ KAPI (sınıf→profil→onay) `gonderim_degerlendir`→`GonderimDurumu` + `GonderimOzeti` şeffaflık + `OnayKaydi` denetim izi. **provenance.rs:** `VeriKokeni` (kaynak/sürüm/BLAKE3/sınıf + `LisansAtif`) + `koken.jsonl`/`onaylar.jsonl`. **lineage_browser.rs:** `KokenGezgini` saf model (ara/detay/**atıflar**). **export.rs HAKLAR:** `veri_envanteri`(erişim)+`tam_veri_ihrac`(taşınabilirlik, manifest filtresiz)+`guvenli_sil`(unutulma; üzerine-yaz+kaldır; proje-değilse reddet). **anonymize.rs:** temel (doğrudan-sil/yarı-genelleştir/k-anonimlik/Laplace-DP). **Yeni dış bağımlılık YOK.** | ✅ | 465/465 (… launcher 33 + **data 58**); fmt/clippy(-D)/machete/topology/**cargo-deny(exit 0)** temiz. +36 gizlilik testi: PHI 5 kanalda engel, fail-closed boş küme, yerel-varsayılan durdurur, **proje global'i ezer**, üç kapı, onay defteri, köken lisans/atıf, gezgin ara/atıf, k-anonimlik, DP deterministik, tam ihraç filtresiz, **güvenli silme + proje-değil reddi**, envanter. Çalışma-zamanı: `--example gizlilik_demo` uçtan uca (PHI engel+onay+köken+ihraç/silme) | Gün 19: İP-09 (Sandbox sertleştirme — OS-düzeyi alt-süreç limiti, MK-12/13/15) |
| 19 | 2026-06-22 | Faz 2 / İP-09 | **Güvenlik, Şifreleme ve Sandbox Sertleştirme — çekirdek güvenlik katmanı (MK-44/45/20). FAZ 2 BİTTİ.** Güvenlik kodu **AÇIK** (Kerckhoffs); kapalı olan yalnız lisans/anti-tamper. **`biocraft-data/src/security/` 6 modül:** **crypto.rs:** `VeriAnahtari` (256-bit, **zeroize** drop'ta sıfırlar, Debug/Clone yok) + `SifreliVeri` zarf; **AES-256-GCM** `sifrele`/`coz` + **her çağrı TAZE rastgele nonce** (getrandom, tekrar yok); kurcalama/yanlış-anahtar **reddedilir** (GCM etiketi). **credentials.rs:** `KimlikDeposu` trait + `OsKimlikDeposu` (**keyring** = Credential Manager/Keychain/keyutils, D-Bus yok) + `BellekKimlikDeposu` (test); anahtar **asla kodda değil**; `anahtar_kur_veya_yukle` kalıcı. **secure_delete.rs:** üzerine-yaz(fsync)+kaldır (kripto-shred notu); privacy `guvenli_sil` bunu kullanır (DRY). **integrity.rs:** `guncelleme_dogrula` Ed25519 imza + BLAKE3 → **sahte güncelleme reddi**. **sanitize.rs:** `pii_temizle` (e-posta/yol/uzun-rakam→`[gizlendi]`). **`plugin-host/harden.rs`:** `AyristirmaLimitleri` (boyut/oran/sayı → **zip-bomb reddi**) + `cikti_denetle` **checked_add** (taşma→hata, panik yok) + `cekirdek_arg_dogrula`; `BcextPaket::ac`'a bağlandı; `SurecSinirlari` (Job Object/cgroup kancası). **`fuzz/`:** workspace-dışı 4 cargo-fuzz hedefi + README. **Yeni dış bağımlılık:** aes-gcm/getrandom/zeroize/keyring (saf Rust, C derleyici yok, Apache/MIT) + ed25519-dalek. | ✅ | 503/503 (… plugin-host **90** + launcher 33 + **data 88**); fmt/clippy(-D)/machete/topology/**cargo-deny(exit 0)** + `--locked` temiz. +38 güvenlik testi: şifrele/çöz + **kurcalama reddi** + **taze nonce** + yanlış-anahtar reddi; OS-depo sakla/al/sil + hex korunur; şeffaf akış uçtan uca; güvenli silme kaldırır/sıfırlar; **sahte güncelleme reddi** (checksum+imza) + bozuk-uzunluk; log PII maskeler; **zip-bomb/oran/taşma(panik-yok) reddi** + arg doğrulama. Çalışma-zamanı: `--example guvenlik_demo` + `eklenti_host_demo [7]` uçtan uca | Gün 20: FAZ 3 — İP-05 (Görsel akış/Node editörü, MK-01) |
| 20 | 2026-06-22 | Faz 3 / İP-05 | **Node Motoru (Görsel Akış Sistemi) — 1. kısım (MK-54). FAZ 3 BAŞLADI.** Unreal Blueprint mantığında node tuvali + tipli portlar + DAG + undo/redo. **`biocraft-ui/src/node/` 7 dosya:** **port.rs:** `VeriTuru` (`uyumlu_mu`=aynı/joker) + **`tur_renk_anahtari`** (FNV-1a→kararlı token anahtarı, renk token'dan=MK-52) + `DonusturucuKayit::oner` (uyumsuzsa **dönüştürücü önerisi**). **graph.rs (saf model):** `Node{durum halkası: Bekliyor/Çalışıyor/Bitti/Hata}`+`Baglanti`+`YapiskanNot`+`PortRef`; **`baglanti_kontrol`** = yön(çıkış→giriş)+aynı-node+**tip uyumu**+**giriş tekildir**(fan-in yok/fan-out serbest)+zaten-var+**döngü reddi**. **dag.rs (`petgraph`'sız):** `yol_var_mi`(DFS)+`topolojik_sira`(Kahn,döngüde None)+`dongu_var_mi`. **commands.rs (undo/redo, `Komut<NodeGraf>` MK-36/37):** NodeEkle/Sil(kenarları da)/Taşı/BağlantıEkle/Sil/NotEkle/Sil — hepsi tek depo `node-graf:<id>`, kararlı yinele. **katalog.rs:** palet + 7 demo genomik node. **canvas.rs (egui):** pan/zoom(imleç sabit)/**minimap**(tıkla-ortala)/tümünü-sığdır/ızgara; **sağ-tık aranabilir palet** + araç çubuğu; **porttan sürükle-bağla** (uyumlu giriş **vurgu**, uyumsuz→**anlık uyarı**+öneri); taşı(undo)/Sil/Delete; bağlı girişe tıkla→sök; **culling+LOD** (MK-04). **app:** Görünüm→**Node Editörü** menüsü merkez tuvali açar (`NodeTuvali::ornek()` demo akış). **Yeni dış bağımlılık YOK.** | ✅ | 537/537 (ui 90→**124** +34); fmt/clippy(-D)/machete/**cargo-deny(exit 0)**+`--locked` temiz. +34 node testi: tip uyumu/joker/öneri + yön/aynı-node + **giriş tekildir** + **döngü reddi(DAG korunur)** + topolojik sıra + DFS; undo/redo uçtan uca (ekle/bağla/taşı/sil + node silince kenar gider/geri yüklenir + **yinele kararlı kimlik**) + MK-37 tek depo; katalog dolu/aranabilir; `tumunu_sigdir` boş/dolu; **headless egui** 2 dil×3 tema+LOD/boş/palet-açık+undo/redo **panik yok**. Çalışma-zamanı: Görünüm→Node Editörü merkez tuvali (demo akış) | Gün 21: İP-05 2. kısım — paralel çalıştırma + önbellek + `.bcflow` + SDK node kaydı + PNG/SVG |
| 21 | 2026-06-22 | Faz 3 / İP-05 | **Node Motoru — 2. kısım — İP-05 TAM (MK-54).** Paralel çalıştırma + sonuç önbelleği + `.bcflow` + eklenti SDK node kaydı + PNG/SVG + node→Python. **`biocraft-sdk/node.rs` kontrat genişledi:** `ParametreTuru/Tanimi/Deger/Parametreler` (kararlı imza) + **`AkisDeger`** (tipli+özet+bayt; kablo önizleme + bütçe) + **`NodeCalistirici` trait** (Send+Sync, `calistir`/`agir_mi`/`tahmini_bayt`) + `NodeKaydi`; `NodeTanimi`'ye serde-default alanlar (eski tanım uyumlu). **`biocraft-ui/src/node/` +4 modül:** **run.rs (motor):** topolojik **dalga** çalıştırma; bağımsız dallar **`std::thread::scope` ile PARALEL** (Rayon/Tokio yok — az bağımlılık ilkesi); **bellek bütçesi** orkestratörden rezervasyon, eş zamanlı grup bütçeye sığacak şekilde → **OOM yok** (tek node bile sığmazsa zarif hata, çökme yok); **hata o dalı durdurur, bağımsız dallar devam**; **iptal jetonu + ilerleme olayı**; `YurutucuKayit::ornek()` 7 demo çalıştırıcı. **cache.rs:** node imzası=tür+parametre+girdi imzaları → değişmeyen **atlanır**, değişen **alt-graf** yeniden (bayat sonuç yok). **serialize.rs:** **`.bcflow` JSON** (sürüm+id+bağlantı+parametre, git-diff) + **göç fonksiyonu** (MK-59, daha-yeni sürüm reddi) + **SVG** (tam vektör+metin) + **saf-Rust minimal PNG** (rasterizer + stored-deflate + CRC32/Adler32, dış bağımlılıksız). **kod.rs:** node→**eşdeğer Python** (iskelet fn+topolojik akış+kwargs; **Kod→Node ters yön YOK** v1.x). **canvas.rs:** **▶ Çalıştır** (arka plan thread → **arayüz DONMAZ**) + İptal/ilerleme çubuğu + **canlı mod** (ağır node uyarısı) + durum halkaları canlı + **kabloya tıkla→ara veri önizleme** + dışa-aktarma menüsü (pano) + F5; senkron `calistir_simdi`. **Yeni dış bağımlılık YOK** (serde/serde_json zaten ağaçta — deny ağacı değişmedi). | ✅ | 571/571 (sdk 7→**11** + ui 124→**154** + …); fmt/clippy(-D)/machete/**cargo-deny(exit 0)**+`--locked` temiz. +34 test: paralel (eş zamanlılık≥2, sleep'li), önbellek değişmeyeni atlar / parametre **alt-grafı geçersiz kılar**, **bütçe aşılmaz OOM-yok** + devasa-node zarif hata + sonda rezerve=0, **hata izolasyonu** (dal durur/bağımsız devam), iptal, arka-plan çalıştırma tamamlanır, **eklenti SDK node kaydı çalışır**, `.bcflow` gidiş-dönüş + daha-yeni-sürüm-reddi + bozuk-net-hata, SVG/PNG geçerli imza, Python yapı, CRC32/Adler32 bilinen değer. Çalışma-zamanı: `--example node_calistir_demo` uçtan uca (8 node paralel+bütçe, 2. çalıştırma 8/8 önbellek, parametre→2 yeniden+6 önbellek, .bcflow 3793 bayt gidiş-dönüş, PNG/SVG/Python dosyaya yazıldı) | Gün 22: İP-06 (Kod Editörü + Hibrit Köprü) |
| 22 | 2026-06-22 | Faz 3 / İP-06 | **Kod Editörü ve Hibrit Köprü — 1. kısım (MK-55, MK-02).** Native editör (Monaco/web YOK; egui). **Vurgulama saf-Rust** (sahip seçimi; Tree-sitter C-kütüphanesi eklenmedi, 'C derleyici GEREKMEZ' korundu; `Vurgulayici` trait → v1.x kancası). **`plugin-host/src/exec/python.rs`:** **out-of-process çalıştırma** (MK-02) — kod **AYRI SÜREÇTE** (`python -u`, PYTHONUTF8), `calistir_baslat` **ANINDA döner** (sonsuz döngü arayüzü dondurmaz), stdout/stderr okuyucu + gözlemci 3 thread; **`durdur()` kill**, zaman aşımı kill, çıktı **satır satır akış** (kayıpsız UTF-8 → Windows cp1252 satır düşmez); `python_bul` paylaşılır (yoksa **in-process'e DÖNMEZ**). **`biocraft-ui/src/editor/` (mod/syntax/tree/run+tests):** `syntax.rs` artımlı saf-Rust (`KodDili` Python+R/Bash/JSON/YAML/RON; üç-tırnak çok-satır durumu; `JetonTuru::token_anahtari` **MK-52 renk token'dan**; `VurgulamaOnbellek` **yalnız değişen satırı** yeniden jetonlar); `mod.rs` `MetinTampon`(String/`AkisGoruntuleyici`), **`AkisGoruntuleyici`** out-of-core **memmap2**+kaba indeks → **1 GB log RAM'siz**, görünür pencere `show_rows`; `Belge`(8 MiB üstü **akış salt-okunur**); `KodEditoru`(sekme+ağaç+kod+çıktı paneli + ▶Çalıştır/▶Hücre/■Durdur; düzenlenebilir egui `TextEdit`+söz dizimi `layouter`; `# %%` hücre seçimi imleçten); `tree.rs` **tembel** ağaç; `run.rs` `CalistirmaDurumu`(kare-başı `yokla` **bloklamaz** MK-07, durdur, 5000-satır taşma koruması). **app:** Görünüm→**Kod Editörü** merkez (node/galeri ile dışlamalı), `KodEditoru::ornek()`. **Yeni dış bağımlılık YOK** (ui→plugin-host L4→L3 [render gibi]; memmap2 zaten ağaçta). | ✅ | 612/612 (ui 154→**189** +35, plugin-host 90→**96** +6); fmt/clippy(-D)/machete/topology/**cargo-deny(exit 0)** temiz. +6 exec testi (Python **canlı**): print akar+Bitti(0); **sonsuz döngü başlatma bloklamaz + Durdur→Durduruldu**; **zaman aşımı→ZamanAsimi(kill)**; hatalı kod çıkış 3; geçersiz yorumlayıcı→Hata. +35 ui testi: tampon/satır; **akış 2500 satır rastgele erişim(kaba indeks)**+salt-okunur+boş-dosya panik-yok; hücre-bul imleçten; sekme aç/kapat+dosya-tekrar-açmaz; vurgu **artımlı yalnız değişen satır**+üç-tırnak alt-satır tetikler+Türkçe tanımlayıcı; layout_job tam kapsar; **headless çizim** 2 dil + akış belge **panik yok**; **uçtan uca gerçek Python run**. | Gün 23: İP-06 2. kısım — node↔kod köprüsü + temel LSP (pyright/jedi) + izole ortam + ruff/black |

> Durum sembolleri: ✅ Tamam · ⚠️ Yarım/TODO var · ❌ Bloke · ⏳ Henüz başlanmadı

---

## Açık TODO'lar (devreden işler)

- (Yapay zeka yarım bıraktığı işleri buraya `// TODO(MK-xx)` referansıyla yazar.)

---

## İnsan Eli Bekleyen İşler (kod dışı — `docs/specs/Hukuk-ve-Operasyon.md`'ten)

> ⚠️ Bunların hiçbiri profesyonel görüş olmadan atılmamalı (Türkiye'de bilişim/fikri mülkiyet avukatı + mali müşavir/SMMM). Sıra, mantıksal önceliğe göredir.

- [ ] **(EN KRİTİK)** "BioCraft Engine" / "BioCraft" marka + domain çakışma kontrolü — TÜRKPATENT + uluslararası (WIPO/Madrid) ön arama. "craft" yaygın olduğundan çakışma riski ciddi araştırılmalı.
- [ ] Domain (biocraftengine.com) + sosyal hesapları kilitle (squatting önlemi).
- [ ] Lisans kararını sabitle: AGPLv3 çekirdek + Apache-2.0 SDK + ticari lisans; **AGPL madde 13 (sunucu) + ayrı-süreç mimarisi + CLA/çift-lisans + MPL-2.0 değerlendirmesi** (hukukçu onayı).
- [ ] Açık/kapalı sınırını netleştir: veri-güvenliği açık; yalnızca premium + lisans/anti-tamper kapalı.
- [ ] Temel hukuki metinler: Kullanım Koşulları (ToS) + Gizlilik Politikası taslağı (hukukçu).
- [ ] İçerik sorumluluğu: haber/pazar/kullanıcı içeriği için bildir-kaldır + moderasyon + ToS maddeleri.
- [ ] KVKK hazırlığı: VERBİS gerekliliği, açık rıza metinleri, ihlal bildirim süreci; sağlık verisi işlenecekse uzman.
- [ ] Klinik/sorumluluk feragati metinleri ("araştırma/bilgilendirme amaçlı, klinik karar için değil").
- [ ] Bilimsel veri lisans uyumu: referans genom (hg38/hg19), dbSNP, ClinVar, UniProt, PDB ticari dağıtımda.
- [ ] Şirketleşme zamanlaması (MVP doğrulanınca): **Limited (LTD)** kuruluşu (mali müşavir) → büyürse A.Ş.
- [ ] Teknokent / TÜBİTAK / KOSGEB Ar-Ge teşvik değerlendirmesi.
- [ ] Kod-imzalama sertifikası (Windows imzalama — tüzel kişilik kurulunca; dağıtım için gerekli).
- [ ] Bio-kredi konumlandırma: "kripto para değil, platform-içi puan"; gerçek ödeme öncesi hukukçu.
- [ ] Çalışan/ortak/katkıcı gelirse: iş/danışmanlık sözleşmesi + IP devri + NDA + CLA.

> (Tamamlananları işaretle. Detaylı çerçeve: `docs/specs/Hukuk-ve-Operasyon.md`. Bu liste gün-gün yol haritasındaki "İnsan Eli İşleri" ile eşleşir.)

> **Not (eski projeden ayrım):** Eski "BioForge" planındaki **Estonya e-Residency / OÜ** yolu ve **IGSC screening API** bu sürümde **kullanılmıyor.** Şirket Türkiye temelli (LTD); biyogüvenlik taraması kapsam dışı.
